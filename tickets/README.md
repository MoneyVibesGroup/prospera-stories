# `tickets/` — les manques découverts **chez l'autre**

## À quoi sert ce dossier

Un **ticket** naît quand une story livrée dans un dépôt découvre un manque qui appartient à **un autre
dépôt**. L'auteur ne peut pas le corriger là où il travaille, et il n'a pas d'autorité pour ouvrir une
story dans le backlog d'en face. Le ticket est le **véhicule de ce passage de main**.

C'est le seul cas légitime. Tout le reste est une **story**.

## Convention de nommage

```
TICKET-<CIBLE>-<sujet-en-kebab-case>.md
```

- `<CIBLE>` = le dépôt qui doit agir, en majuscules : `BACKEND` · `FRONTEND` · `ADMIN` · `OPS`.
  **C'est la cible, pas l'origine** — un ticket se range par qui doit le traiter.
- `<sujet>` = le symptôme, en kebab-case, sans numéro de story (l'origine se lit dans l'en-tête du
  fichier et dans `git log`, elle n'a rien à faire dans le nom de fichier : un ticket peut être
  découvert deux fois, il n'a qu'une cible).

## Cycle de vie — un ticket est un état transitoire, pas une destination

1. **Ouvert** — l'auteur crée le fichier ici et **l'inscrit dans le tracker de la cible**
   (`sprint-status.yaml` → `open_contract_gaps`, ou le tracker frontend). Un ticket qui n'est pas
   dans un tracker est **invisible du sprint-planning**, et c'est exactement comme ça que
   `TICKET-fe-026` a dormi deux jours et que STORY-133/134/135/136/144 sont devenues orphelines.
2. **Repris** — dès qu'il a un porteur, il devient une **story numérotée** dans le backlog de la
   cible. Le ticket est alors stampé en tête (« repris par X ») et **X devient la source de vérité**.
3. **Résolu** — la story est livrée ; le ticket est stampé « résolu par X ».

⚠️ **Un ticket stampé ne se modifie plus.** Il est conservé pour tracer l'origine — pas pour être
maintenu en parallèle de la story. Deux sources de vérité sur le même sujet, c'est précisément le
défaut que ce dépôt a rencontré trois fois (statuts périmés, stories orphelines, 134/135/136 vs 144).

## État au 2026-08-15

| Ticket | Cible | Ouvert par | État |
|---|---|---|---|
| `TICKET-BACKEND-dossier-client-entite-de-premier-rang.md` | backend *(+ volet frontend)* | question du PO (barry thierno alhassane, 09/08) | ➡️ **REPRIS le 09/08 par 13 stories `EPIC-043`, toutes au SPRINT 20** (8 créées : 353→360 · 5 **réancrées** : 236 du S22, 301/302/304 du S23, 303 du S24). **73 pts.** Les stories font foi. Décision PO : fermer balance+bilan d'une société **avant** que la fiscalité ne démarre au S22. — Historique : 🟠 décisions D1→D16 prises, découpage suspendu. ⚡ Exception au cycle ci-dessus : ce ticket ne naît pas d'une story mais d'une **décision produit** qui renverse une invariante livrée (« une organisation = une société », `profil-societe.schema.ts`). Il porte **16 blocs** de travail A→P, non numérotés **volontairement**. ⬆️ **2ᵉ entrée du 09/08** — maquette validée : blocs **J** (aucun `GET` n'existe sur les exercices), **K** (vue consolidée + compteurs de portefeuille), **L** (audit du profil **écrit mais illisible** — répétition de STORY-144), **M** (régime non daté par exercice). ⬆️ **3ᵉ entrée du 09/08** — analyse de la partie dossier, 6 décisions D11→D16 : blocs **N** (⚡ `notification-service` **n'existe pas** alors que « l'admin est informé » en dépend), **O** (unicité du NIF société, aucun index aujourd'hui), **P** (échéance au portefeuille — la donnée existe dans le paquet fiscal, aucune route ne la sert). Q3/Q4/Q5 tranchées, **Q6→Q12** ouvertes |
| `TICKET-BACKEND-mapping-profile-accepte-mais-ignore.md` | backend | FE-025 (barry thierno alhassane, 25/07) | ✅ **résolu** par STORY-088 — reste la régénération des types, portée par FE-057 |
| `TICKET-FRONTEND-retrait-dictionnaire-plan-comptes.md` | frontend | STORY-139 (vivianMoneyVibesGroupes, 29/07) | ✅ **résolu** par **FE-056** le 2026-08-07 (commit `c1b777f`) |
| `TICKET-BACKEND-ecarts-releves-par-integration-gate-console.md` | backend | Integration Gate console (03/08) | ➡️ voir `GAP-integration-gate-console` |
| `TICKET-BACKEND-ap-int-1-revue-kyc-sans-document.md` | backend | AP-INT-1 (04/08) | ➡️ **repris** par STORY-179→184 (sprint 20) |
| `TICKET-BACKEND-console-inexercable-faute-de-donnees.md` | backend | console (05/08) | ➡️ voir `GAP-console-inexercable-faute-de-donnees` |
| `TICKET-BACKEND-referentiels-attribuables-mais-non-servis.md` | backend + console | maquette **FE-056** (barry thierno alhassane, 07/08) | ➡️ **repris** — ① par **STORY-292**, ③ par **STORY-293** (sprint 20) ; ② clos sans action |
| `TICKET-BACKEND-tag-referentiel-non-expose.md` | backend | **FE-056** à l'intégration (barry thierno alhassane, 07/08) | ⛔ **ouvert** — aucune route ne donne au client le tag `SN\|SMT\|SFD-BCEAO` que `POST /balances` exige |
| `TICKET-OPS-atelier-injoignable-depuis-un-navigateur.md` | ops (+ backend) | **FE-057** à l'Integration Gate (barry thierno alhassane, 08/08) | ✅ **corrigé** le 08/08 — ① CORS ajouté à `balance-service` **et** `bilan-service` (7/7 services au préflight) ; ② `.gitattributes` (`-text`) posé dans `balance-service` et `bilan-service` — dont les 5 artefacts étaient **déjà** en CRLF, défaut actif sans témoin. Les deux pannes étaient **invisibles au curl**. Reste : relire et pousser les 2 branches `fix-gitattributes-artefacts-checksum` |
| `TICKET-BACKEND-objets-imbriques-non-types-dans-l-openapi.md` | backend | **FE-057** à la régénération (barry thierno alhassane, 08/08) | ⛔ **ouvert** — 33 objets déclarés par `@ApiProperty({ example })` sans schéma sortent en `Record<string, never>` ; le front renonce à afficher `referentiel{code,version}` et `paquetFiscal{pays,annee}` que le serveur envoie pourtant |
| `TICKET-BACKEND-journal-d-audit-des-organisations-non-lisible.md` | backend | **AP-20** à l'intégration (barry thierno alhassane, 07/08) | ➡️ **repris** le 08/08 par **STORY-294** (sprint 20) — consommateur **AP-24** nommé en même temps, pour ne pas rejouer l'orphelinat de STORY-144. `admin_audit_logs` est écrit transactionnellement, **aucune route ne le relit** ; 3 arbitrages portés par la story |
| `TICKET-FRONTEND-regeneration-types-cima-story-292.md` *(doublon de ligne, voir ci-dessus)* | frontend | **STORY-292** à la livraison (vivianMoneyVibesGroupes, 10/08) | ➡️ **repris par AP-28 le 15/08, RÉADRESSÉ à FE-070 le 18/08** — `REFERENTIELS_BALANCE` gagne `CIMA` côté backend (additif) ; les types générés côté front restaient figés à 3 valeurs. ⚠️ AP-28 visait `frontend-admin-panel`, **qui ne consomme pas `balance-service`** ⇒ fermée sans objet. **FE-070 fait foi** (dépôt `prospera-frontend-expert-comptable`) |
| `TICKET-BACKEND-classes-de-gestion-non-sourcees-par-referentiel.md` | backend *(2 dépôts)* | **revue de code de STORY-292** (vivianMoneyVibesGroupes, 10/08) | ⛔ **ouvert** — `CLASSES_DE_GESTION=[6,7,8]` **ment** pour CIMA (classe 8 = gestion **+** `87`/`88`/`89` de regroupement) ⇒ résultat comptable **doublé** (280 M au lieu de 140 M, mesuré), et l'articulation qui le pincerait est inapplicable ⇒ **silence total**. Latent (aucune org CIMA, plan suspendu à AC-18). Correction = sourcer les classes dans l'artefact ⇒ `build.mjs` de `bilan-service` |
| `TICKET-FRONTEND-decision-kyc-impossible-sans-if-match.md` | frontend *(console)* | STORY-184 a l'e2e navigateur (11/08) | ➡️ **REPRIS le 15/08 par AP-26** (sprint 9, 2 pts, en TÊTE de sprint) — la console rend `428` sur TOUTE décision KYC depuis STORY-182 (07/08) : l'acte central de la console est inopérant. AP-26 fait foi |
| `TICKET-FRONTEND-reference-dossier-kyc-et-compteur.md` | frontend *(console)* | STORY-184 (11/08) | ➡️ **REPRIS le 15/08 par AP-27** (sprint 9, 2 pts) — `reference` et `nombreSoumissions` sont servis et requis au contrat ; la console affiche encore l'`orgId`, filigrane de la visionneuse compris. AP-27 fait foi |
| `TICKET-FRONTEND-regeneration-types-cima-story-292.md` | frontend | STORY-292 (10/08) | ➡️ ✅ **RÉSOLU le 18/08 par FE-070** (sprint 9, 2 pts, dépôt `prospera-frontend-expert-comptable`) — remplace l'entrée « ⛔ ouvert » ci-dessus. ⚠️ **Repris le 15/08 par AP-28, qui visait la MAUVAISE APPLICATION** : la console `frontend-admin-panel` n'a ni `balance` dans `gen-api.mjs`, ni `NEXT_PUBLIC_BALANCE_URL`, ni un seul fichier contenant `SFD-BCEAO` ⇒ AP-28 **fermée sans objet**, ses 2 pts transférés. Le livrable réel n'était d'ailleurs PAS la régénération mais le sélecteur en dur `["SN","SMT","SFD-BCEAO"]` de `saisie-directe-view.tsx:323`. **FE-070 fait foi** |
| `TICKET-BACKEND-dirigeants-et-associes-hors-regime-salarial.md` | backend *(PRD + epics + spine fiscalité)* | revue de couverture du module employé, demandée par le PO (15/08) | ⛔ **OUVERT — arbitrage PO requis AVANT le découpage d'EPIC-034.** `FR-F27` fonde la base sociale « par salarié » ; un gérant majoritaire n'en est pas un. ⚡ Le paquet fiscal cite pourtant l'Art. 75 (`irpp.source`) : le référentiel connaît la règle, le PRD ne l'a jamais traduite. + revenus distribués Art. 79 sans FR. + 2 constats connexes : **EPIC-034 n'a aucune AD** (`application/remuneration` cité une seule fois, rattaché à AD-9/AD-10 qui ne parlent pas de rémunération) et **la paie est de la donnée personnelle** sans règle de conservation ni de minimisation |
| `TICKET-BACKEND-filtre-mes-dossiers-au-portefeuille.md` | backend | **FE-059a** à l'intégration (18/08) | ✅ **FERMÉ le 2026-08-20** — arbitrage PO du **2026-08-19** : Q1 *maintenu* · Q2 *responsable ∪ contributeur* · Q3 *choix porté par l'URL* ; servi par **STORY-359** (`?affectation=moi`, appliqué APRÈS la portée du jeton, en `$and`) et **consommé par FE-071 le 2026-08-23**. ⚠️ Cette ligne a dit « OUVERT » pendant 3 jours après la fermeture, pendant que `sprint-status.yaml` disait l'inverse — et la fiche FE-071 s'appuyait dessus pour rester `blocked`. — Contexte d'origine — La maquette **FE-D00 validée** porte un sélecteur `data-admin-only` « Tous les dossiers / Mes dossiers » ; `GET /dossiers` n'accepte **aucun** paramètre (OpenAPI réel : `parameters: []`). FE-059a a livré le portefeuille **sans**, et l'a écrit dans le code plutôt que de le simuler. ⚡ **Ce n'est PAS une demande de portée** — le bloc **K** verrouille « portée dérivée du jeton, jamais un paramètre de requête » et **D11** garde « Mon cabinet » invisible d'un collaborateur ; le filtre est **orthogonal**, il agit *à l'intérieur* de ce que le jeton autorise déjà. ⚠️ **Infaisable côté client** : **D16** impose la pagination serveur (STORY-359) ⇒ un filtre local ne porterait que sur **la page courante** — le défaut même que la maquette proscrit pour les compteurs. **Consommateur nommé : FE-071** *(blocked)*, pour ne pas rejouer l'orphelinat de STORY-144. **À traiter AVEC STORY-359**, même route |
| `TICKET-BACKEND-balance-ne-declare-pas-son-dossier.md` | backend | **FE-063** à l'intégration (19/08) | ⛔ **ouvert** — STORY-236 rend le `dossierId` obligatoire **en entrée** (22 contrôleurs sous `/dossiers/:dossierId/…`, guard, index d'unicité) mais `BalanceResponseDto` ne le publie **pas en sortie** — alors qu'il publie `orgId`, c'est-à-dire la portée qui n'est PLUS l'unité de travail. ⚡ Conséquence : **le front ne peut pas vérifier ce qu'il affiche** — « bon dossier », « autre dossier » et « agrégat tous dossiers » sont indiscernables dans le corps. Seule l'URL qui a demandé la réponse la rattache à un dossier, donc une propriété de la REQUÊTE, jamais de la donnée. C'est le risque nº2 sans témoin possible. ⚠️ Pas théorique : FE-063 l'a rencontré **dans son propre outillage e2e** (stub ancré sur le chemin et non sur l'origine ⇒ une liste de dossiers servie en `200` pour une demande de balances, indétectable côté client). ⚠️ **Ne PAS sceller le champ dans le checksum** — le sceau ne couvre que le contenu métier ; l'y ajouter refuserait en `400` toute balance soumise. Consommateur front : la garde de FE-063 protège l'ÉMISSION, pas la réception |
| `TICKET-BACKEND-fiche-dossier-trois-surfaces-annoncees-inexistantes.md` | backend | **FE-061** au réancrage (19/08) | ⛔ **ouvert** — `dossier-service` sert **10 chemins**, et la fiche de FE-061 en annonçait **trois de plus** : `PATCH /dossiers/{id}` (identité), `GET /{id}/completude`, et la lecture de l'**attestation de mandat**. ⚡ Leurs substituts apparents sont des **pièges** : `PATCH`/`GET /profil-societe` de `:3007` sont **org-keyed** ⇒ ils auraient affiché l'identité et la complétude **du CABINET** sur la fiche d'un **CLIENT**, c'est-à-dire « une org = une société » recâblé par la porte de derrière. L'attestation, elle, est une **ligne de journal** (D2) que `DossierResponseDto` ne publie pas et qu'aucune route ne relit — 3ᵉ répétition de STORY-144. ⇒ FE-061 livre l'identité **en lecture**, sans complétude ni bandeau de mandat, et le DIT à l'écran. ⚠️ Le verrou optimiste est à **déclarer au contrat** : la fiche supposait un `412` sur `If-Match` que le service ne produit pas (verrou interne, `409 CONFLIT_CONCURRENT`) |
| `TICKET-BACKEND-referentiel-effectif-non-resolu-par-dossier.md` | backend | **FE-061** au réancrage (19/08) | ⛔ **ouvert** — la **famille** de référentiel est décidée **par dossier** (`typeEntite` ⇒ D7/STORY-304), ses **versions** sont servies **par organisation** (`GET /referentiels/actifs`, `:3007`). ⇒ **aucune façon de répondre à « quelle version du plan de comptes s'applique à CE client ? »**. ⚡ Le cas nominal le casse : un cabinet tenant une entreprise (`SYSCOHADA`) **et** une microfinance (`SFD-BCEAO`) verrait le même paramétrage sur les deux fiches, dont une où il est faux — **exactement le défaut que STORY-303 vient de fermer côté axes**, réintroduit par l'affichage. ⇒ FE-061 **nomme** le référentiel et **omet** ses versions. ⚠️ Ne PAS re-scoper la route existante : elle est aussi la **sonde d'autorité** du gate Atelier (FE-063, nuance D5) — demander une route sœur `GET /dossiers/{id}/referentiels/actifs`, qui **déclare sa portée** dans son corps |
| `TICKET-BACKEND-portefeuille-ne-porte-plus-l-etat-des-axes.md` | backend | **FE-061**, retombée de **STORY-303** (19/08) | ⛔ **ouvert** — STORY-303 retire `systemeComptable`/`regimeFiscal` de `DossierResponseDto` (retrait **justifié**), mais la LISTE en dépendait : puce « Axes à déterminer », tri « à traiter d'abord », ligne du bandeau de contexte. La seule source restante est `GET /dossiers/{id}/axes` — **une requête par dossier**, sur une cible NFR de **500 dossiers**. ⚡ Les garder aurait allumé l'alerte sur **TOUS** les dossiers, y compris ceux parfaitement configurés : une alerte généralisée est **pire** que pas d'alerte. ⇒ puce et tri **RETIRÉS par FE-061** (perte de fonction assumée, tracée ici pour ne pas être redécouverte comme un oubli de FE-059a). Demande : un booléen dérivé `axesDetermines` sur le DTO — **sans jamais republier le couple**, ce qui rouvrirait la divergence |
| `TICKET-FRONTEND-preuve-par-les-chiffres-de-fe-063-injouable.md` | frontend | **FE-061**, suite e2e complète (19/08) | ⛔ **OUVERT — 2 tests ROUGES sur `dev`, donc toute PR frontend hérite d'une CI rouge.** `atelier-scope-dossier.spec.ts` cherche un montant (`452 000`) dans le **tableau des balances** ; `balances-list-view.tsx` rend six colonnes dont **aucune ne porte de montant** (il l'écrit lui-même : « pas de total exposé par le service : on ne l'invente pas »). L'assertion vise la LISTE, les montants vivent dans le DÉTAIL ⇒ elle n'a **jamais** pu passer. ⚡ **Ce n'est pas qu'un test rouge** : le tracker enregistre FE-063 comme ayant prouvé la bascule A→B « **PAR LES CHIFFRES** », et c'est précisément l'assertion qui échoue — les assertions négatives voisines passent **par vacuité** (elles ne voient aucun chiffre nulle part, donc resteraient vertes même sur le mauvais dossier). La preuve réellement portée est l'**exercice** + l'**inspection du réseau**, ce qui est solide mais n'est pas ce qui a été consigné. ⚠️ Le « 588/588 » de FE-063 portait sur **Vitest** ; Playwright n'a pas été rejoué avant le merge. **FE-061 n'y a délibérément pas touché** : l'absorber aurait masqué qu'une story a été mergée sur une preuve qu'elle n'avait pas |
| `TICKET-BACKEND-bilan-ne-reference-pas-sa-balance-source.md` | backend | **FE-028** à la conception de l'écran de choix de la balance source (barry thierno alhassane, 22/08) | ➡️ **REPRIS le jour même par STORY-381** (5 pts, sprint 20). ⚡ `bilan-service` ne connaît **aucun `balanceId`** : la liasse ne peut pas dire d'où elle vient, et le serveur ne peut refuser ni un brouillon ni les soldes d'un autre dossier. Le consommateur de `balance.created` était « différé EPIC-009 » — un épic **`superseded` cinq jours avant** la livraison du producteur (STORY-099) |

### Ce qui a été corrigé le 2026-07-31

Les deux fichiers vivaient dans `stories/` — **le dossier des stories backend** — alors que l'un des
deux est un ticket *frontend*. Ils suivaient deux schémas de nommage différents
(`TICKET-<story>-<sujet>` vs `TICKET-<sujet>`) et deux casses différentes (`fe-026` vs
`mappingProfile`), et **aucun des deux n'était référencé par un tracker**.
