---
scope: micro-service notification-service (Module 1 — Canaux & notifications) — organe de parole unique de Prospera : carnet de contacts, modeles versionnes, 5 canaux derriere ChannelProvider, envoi transactionnel et de masse, journal + accuses, reponses entrantes, consentement, mesure de consommation, retention
purpose: build-substrate
altitude: feature
updated: 2026-08-04T10:49
---

- (constraint) Herite ecosystem P3/P4 : relying-party JWKS RS256 valide localement, read-models locaux alimentes par Kafka (identity.*, kyc.status.changed, entitlement.changed), jamais d'appel synchrone a un autre service sur le chemin chaud.
- (constraint) Herite ecosystem : orgId vient du jeton signe ; database-per-service ; outbox transactionnelle ; partition Kafka = orgId ; moule unique P12 (NestJS, abstraction provider par capacite externe).
- (constraint) Herite ecosystem P6 : Kafka = evenements de domaine inter-services ; Redis/BullMQ = jobs internes a un service. Le PRD FR-N24 fait de notification-service un CONSOMMATEUR d'evenements metier — premier service du programme dont l'entree principale est le bus, pas l'API.
- (constraint) Herite paiement-service spine AD-10/AD-12/AD-17 : journal protege par base Mongo separee (privileges Mongo additifs et sans deny), aucun setInterval — tout fait temporel est un job BullMQ a cle idempotente, notification-service est deja nomme organe de parole unique par AD-17.
- (constraint) Herite paiement-service Consistency Conventions : les donnees du destinataire sans compte Prospera suivent le regime du carnet de contacts de notification-service (aucun profil constitue au-dela de l'envoi qui l'a cree). Cette spine doit donc HONORER ce renvoi, pas le re-decider ailleurs.
- (constraint) Constat de code 2026-08-03 : auth-service/src/modules/mail/ = MailerService nodemailer + 7 templates Handlebars .hbs compiles depuis le disque + mail.processor.ts sur file BullMQ MAIL_QUEUE. C'est la dette FR-N27/SM-1 a solder. Le patron file+processor est ratifiable ; les templates .hbs sur disque ne le sont PAS (FR-N10 exige des modeles versionnes en base, editables par le client).
- (constraint) Constat docker-compose racine 2026-08-03 : ports pris 3000 expert-comptable, 3001 auth, 3002 kyc, 3003 platform-catalog, 3004 bilan, 3006 document, 3007 balance, 3010 admin-panel. Reserves par spines : 3005 paiement, 3011 assistant-ia, 3012 fiscal. 3008 et 3009 LIBRES — notification-service prend :3008.
- (event) Mode retenu : coaching (choix user). Livrables : ARCHITECTURE-SPINE.md + memlog, puis enchainement epics/stories (EPIC-027). Pas de doc compagnon narratif, pas de deck.
- (decision) AD candidat (choix user) — Deux niveaux, pas deux agregats paralleles : Envoi = la remise unitaire (unite du journal, de l'accuse, du cout — exactement le glossaire du PRD) ; EnvoiDeMasse orchestre et produit N Envoi. Binds §1.3, FR-N29/N30/N35/N38/N39/N59. Prevents : les deux defauts symetriques du §1.3 (promotion sous regime service / mise en demeure bloquee par un desabonnement marketing). Regle qui les ferme : la ature n'est JAMAIS un parametre d'appelant, elle nait du point d'entree — aucun cas d'usage n'accepte ature en entree.
- (decision) Choix user CONTRE ma recommandation sur FR-N30 : curseur + point de reprise plutot que materialisation complete a la preparation. Motif presume : eviter N ecritures avant le premier message. Les deux trous nommes dans les options restent a fermer — (a) crash en plein lot = zone grise sans cle d'idempotence par destinataire, (b) FR-N28 laisse un module alimenter une liste, donc un index ne designe plus la meme personne d'un lot a l'autre. Proposition de reconciliation au tour 2 : materialisation PAR LOT (le curseur avance, mais chaque lot ecrit ses lignes avant d'envoyer) — meme profil de cout que le curseur, garantie FR-N30 preservee.
- (decision) AD candidat (choix user) — Les variables du message ont leur PROPRE horloge de retention, plus courte que le journal : journal 13 mois (§9.2), variables purgees a 90 jours (parametrable, plafonne par FR-N64). Binds FR-N35, FR-N40, FR-N51, §9.2, §9.3, FR-N64/N65/N66. Prevents : un journal presente comme minimise alors qu'il porte {montantDu, nom} pendant 13 mois. Consequence : la fenetre de rejeu manuel (FR-N40) est BORNEE a 90 jours et doit etre dite comme telle ; au-dela le journal ne garde que le squelette (destinataire, modele@version, canal, statut, cout).
- (question) DEFAUT TROUVE DANS LE PRD — §9.3 affirme que ne pas conserver le rendu evite de dupliquer les variables sensibles dans le journal, alors que FR-N35 journalise explicitement les variables. Stocker {montantDu, nom} est aussi personnel que stocker le texte rendu. La decision d'horloge courte rend §9.3 vrai apres 90 jours ; le §9.3 du PRD reste a corriger.
- (decision) AD candidat (choix user) — DEUX chemins d'entree, discrimines par le CONTENU et non par l'appelant : un message porteur d'un secret (lien a usage unique, code) entre par APPEL DIRECT authentifie machine-a-machine ; tout autre declencheur entre par EVENEMENT consomme sur le bus. Binds FR-N23, FR-N24, FR-N25, FR-N27, SM-1. Prevents : un jeton de verification ou de reinitialisation de mot de passe depose sur un topic Kafka durable, rejouable depuis l'offset 0, lisible par tout consumer group du programme et present dans les sauvegardes.
- (constraint) CONDITION BLOQUANTE portee par l'AD des deux chemins : C8 (authentification machine-a-machine entre services) est une decision PROGRAMME encore OUVERTE — la spine paiement-service l'a explicitement contournee par evenement (AD-13) et note qu'elle reste ouverte pour d'autres appelants. Ici elle ne peut pas etre contournee : FR-N27 exige que auth-service cesse d'envoyer, et ses messages portent des secrets. C8 doit etre tranchee AVANT l'increment 2. Hors de l'autorite de cette colonne.
- (question) Sous-question ouverte du chemin par appel direct : que fait auth-service si notification-service repond 503 pendant une inscription ? Proposition a valider — l'inscription N'ECHOUE PAS ; le client sortant de auth reessaie avec backoff (une file de reessai n'est pas de la logique d'envoi, donc SM-1 reste a 0 : ce n'est ni un appel SMTP ni un appel de canal), et tout message porteur d'un secret expose un chemin de renvoi a l'initiative de l'utilisateur.
- (decision) AD candidat (choix user, revient sur le curseur nu) — Envoi de masse : la liste est FIGEE en instantane a la preparation, le curseur avance lot par lot, et chaque lot ecrit ses lignes Envoi{prepare} en insertMany ordered:false AVANT de remettre au canal, sous index unique (envoiDeMasseId, contactId, canal). Binds FR-N29/N30/N31/N32/N33/N48, NFR-2. Prevents : un arret en plein lot qui rejoue jusqu'a 500 remises deja faites, et un index de liste qui ne designe plus la meme personne d'un lot a l'autre (FR-N28 laisse un module alimenter une liste). Profil de cout identique au curseur nu ; garantie FR-N30 preservee. Le consentement est RE-verifie a la remise, pas seulement a la preparation (FR-N48).
- (decision) AD candidat (choix user) — Repli de canal FR-N21 : UN Envoi PAR CANAL TENTE, chaines entre eux. L'Envoi reste l'unite du journal, de l'accuse et du cout, conformement au glossaire du PRD et a la decision d'agregat. FR-N38 restitue a l'appelant l'etat TERMINAL de la chaine. Consequence a porter en convention : la contre-metrique CM-1 (volume moyen recu par destinataire) compte les Envoi DELIVRES, jamais les Envoi crees — sinon un repli gonfle artificiellement le compteur.
- (decision) AD candidat (choix user) — Le RATTACHEMENT d'une reponse entrante porte un niveau de certitude, exactement comme le statut de lecture de NFR-1 : CONFIRME (le canal transporte une reference au message d'origine), PRESUME (inference par contact + canal + derniere conversation ouverte), ABSENT (destination par defaut FR-N43). Binds FR-N41/N42/N43/N44/N45. Prevents : une reponse devinee presentee a un module comme un fait. MIROIR DE NFR-1b : aucun module ne declenche d'automatisme sur un rattachement PRESUME — il peut l'afficher a un humain, pas en deduire une promesse de paiement.
- (decision) AD candidat (choix user) — Modeles FR-N11 : chaine de resolution (modele de l'org sinon modele systeme orgId=null), resolue A LA PREPARATION de l'envoi et FIGEE sur l'Envoi. Une seule copie du socle. Prevents : une nouvelle version systeme qui modifierait un envoi deja prepare ou reecrirait le journal, et la perte de filiation d'une copie a la surcharge. Coherent avec FR-N10 (immuable une fois utilise).
- (decision) Tranche sans question (herite, non conteste) — (a) le canal in-app ne cree AUCUN contact miroir : le Destinataire d'un Envoi est polymorphe Contact|Utilisateur, car un contact miroir tomberait sous la purge du §9 et sous le desabonnement alors que l'identite appartient a auth-service ; (b) le cout d'envoi reutilise le referentiel pays-devises-ao@AAAA.N de platform-catalog-service (paiement AD-8), pas un second registre ; (c) faits temporels et purges = travaux BullMQ a cle idempotente, aucun setInterval (fiscal AD-18, paiement AD-12).
- (decision) AD candidat (choix user) — Boite de reception d'accuses append-only + statut DERIVE : chaque accus de passerelle est persiste brut (signature verifiee AVANT persistance, cle d'unicite propre), et le statut de l'Envoi est la projection du PLUS AVANCE des etats observes, jamais le dernier recu. Ordre du domaine prepare < envoye < delivre < lu < repondu. Binds FR-N36, FR-N37, FR-N45, NFR-1a, NFR-4. Prevents : un statut qui recule de lu a delivre quand WhatsApp emet ses deux accuses en rafale inversee — le saut arriere que FR-N36 interdit — et la perte de l'information de lecture, justement l'exigence prioritaire du client au memlog du PRD. Miroir de la boite de reception paiement AD-4.
- (decision) AD candidat (choix user) — Normalisation de l'identifiant de canal : l'organisation declare son indicatif par defaut (issu de son pays dans le read-model identity.org), SURCHARGEABLE par import — necessaire des qu'une org opere dans plusieurs pays (FR-N57c le prevoit deja). La forme normalisee est STOCKEE et porte la cle unique ; la forme brute saisie est conservee a cote ; le compte rendu FR-N08 montre les deux AVANT persistance. Binds FR-N02, FR-N03, FR-N06, FR-N07, FR-N08. Prevents : un numero beninois normalise en +228 par une org togolaise multi-pays — deux fiches pour une personne, ou une fusion a tort, et dans les deux cas un echec SILENCIEUX du dedoublonnage.
- (question) AMBIGUITE A LEVER (choix user 'preuves + journal d'envoi' dans la base protegee) : FR-N35 ne decrit PAS un journal separe — il decrit les champs de l'Envoi lui-meme (destinataire, canal, modele+version, variables, moduleAppelant, horodatages, statut, cout). Si 'le journal' = la collection envois, la mettre en find+insert seul casse trois FR : le statut projete depuis les accuses (FR-N36) exige update, la purge a 13 mois (FR-N65) et l'agregation anonyme (FR-N66) exigent remove. Deux lectures possibles, materiellement differentes — question posee au user.
- (decision) AD candidat (choix user, leve l'ambiguite) — DEUX journaux distincts, pas un. (1) L'Envoi reste OPERATIONNEL dans notification_service : statut projete depuis les accuses, variables purgees a 90 j, detail purge a 13 mois puis remplace par agregats anonymes, filtrable/exportable/rejouable (FR-N35/N39/N40/N65/N66). (2) Chaque remise ecrit EN PLUS une entree d'audit append-only dans notification_service_preuves — orgId, moduleAppelant, identifiantCanal, canal, modele@version, cout, horodatage — SANS variables et SANS rendu, donc sans donnee personnelle au-dela de l'identifiant de canal. Elle SURVIT a la purge des 13 mois. Binds FR-N52, FR-N68, §9.2. Prevents : un module futur qui efface la trace de ce qui a ete envoye, et l'impossibilite de prouver qu'on a bien parle une fois le journal purge. Patron paiement AD-10. A fixer au tour de distillation : la duree de conservation de l'audit, au-dela des 13 mois du journal.
- (event) Fin de la phase de coaching — 3 tours, 12 decisions structurantes prises par l'utilisateur. Passage a la distillation de la spine.
- (decision) AD candidat (verifie sur le web 2026-08-04, non conteste) — Le rendu est une SUBSTITUTION DE VARIABLES DECLAREES, jamais la compilation d'un programme. Motif : FR-N12 exige que le client ecrive ses modeles, et compiler un template Handlebars de source non fiable est une surface RCE documentee (CVE-2021-23369 ; la recommandation universelle est de ne JAMAIS compiler un modele non fiable). Handlebars reste utilisable pour la mise en page HTML systeme livree AVEC le code, jamais pour un modele venu de la base. Cout assume : pas de conditionnel ni de boucle dans un modele client — une variante conditionnelle se fait par deux modeles et une regle de declenchement.
- (version) Stack ratifiee depuis le code du depot le 2026-08-04 (brownfield, on aligne) : NestJS 11, Mongoose 8.24, Node types 22, TypeScript 5.7, kafkajs 2.2.4, bullmq 5.79 + ioredis 5.11 + @nestjs/bullmq 11.0.4 (auth-service), nodemailer 6.9.16 + handlebars 4.7.8 (auth-service), nestjs-cls 6.2, nestjs-pino 4.6, jwks-rsa 3.2, helmet 8, @nestjs/throttler 6.5, @nestjs/terminus 11, class-validator 0.14, Jest 29. Seuils de couverture du moule : 65/90/90/90.
- (question) Ouverte, a confirmer par le PO : duree de conservation de la trace d'audit d'envoi (objet nouveau, absent du PRD). Proposition alignee sur la preuve de consentement du §9.2 — 3 ans apres la fin de la relation, plafond 5 ans. Elle survit par construction a la purge du journal a 13 mois.
- (event) Reviewer Gate execute — lint_spine.py : 0 finding, deux fois. Revue semantique menee en sequentiel (les regles de session interdisent de lancer des sous-agents sans demande explicite ; le gate prevoit ce mode). Lentilles appliquees : verification des versions, adversarial (deux unites conformes qui divergent quand meme), checklist de bonne spine, couverture du PRD, enveloppe operationnelle. 9 trous trouves, 9 corriges dans la spine.
- (decision) Correctif de gate [CRITIQUE] — NFR-3 n'etait couvert nulle part : un envoi de masse de 50 000 destinataires sur la file commune retarderait un code de verification de plusieurs heures alors que NFR-3 lui impose P95 < 10 s. Regle ajoutee a AD-13 : TROIS files BullMQ distinctes (transactionnel-prioritaire, transactionnel, masse), jamais fusionnees, et la file est determinee par la nature d'AD-1 — jamais choisie par l'appelant.
- (decision) Correctif de gate [CRITIQUE] — FR-N61 (vue plateforme toutes organisations) contredisait NFR-5 (cloisonnement) sans que rien ne l'arbitre : deux unites auraient diverge entre un if PLATFORM_ADMIN dans le depot et une lecture agregee separee. Regle ajoutee a AD-16 : la vue plateforme lit EXCLUSIVEMENT des compteurs pre-agreges, et aucun chemin de code ne rend l'orgId facultatif sur une collection operationnelle.
- (decision) Correctifs de gate [HAUT] — (a) AD-3 : la cle d'idempotence (orgId, eventId, canal) aurait AVALE le second envoi quand un meme evenement en declenche legitimement plusieurs (kyc.status.changed -> e-mail au dirigeant ET in-app au gestionnaire) ; cle etendue a (orgId, cleIdempotence, regleDeclenchementId, destinataireRef, canal). (b) AD-10 : un 'STOP' SMS est un message ENTRANT — sans regle il partait chez Relance comme une reponse ordinaire et le refus restait sans effet ; il est desormais intercepte AVANT la cascade de rattachement. (c) AD-14 : l'entree d'audit vit dans une autre base — ecrite dans la MEME transaction Mongo que la transition vers envoye, ce qui est la raison pour laquelle la base protegee partage le replica set rs0.
- (decision) Correctifs de gate [MOYEN/BAS] — (d) AD-15 : la retention des accuses n'etait pas dite ; ils suivent le journal a 13 mois, sans horloge propre. (e) AD-7 : 'etat terminal de la chaine' etait indefini ; c'est l'etat du DERNIER MAILLON, pas le max de tous. (f) Inherited Invariants : le schema registry P9 (compatibilite BACKWARD en CI) manquait. (g) Images docker verifiees dans le docker-compose racine au lieu d'etre reprises de la spine paiement : mongo:7, apache/kafka:3.9.0, redis:7-alpine, mailhog/mailhog — toutes confirmees.
- (event) spine finalized
