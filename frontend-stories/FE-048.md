# Story FE-048 : Profil d'import & mapping de colonnes réutilisable (Sage)

Status: blocked (backend non livré)

**Epic :** FE-EPIC-007 — Atelier Balance
**Points :** 3 · **Sprint :** 13 (programme) · **App :** `prospera-frontend-expert-comptable`
**API :** balance-service (`:3007`) — `POST /balance/import/sage` (champ `mappingProfile`) + CRUD profils (STORY-088)
**Backend d'appui :** **STORY-088** — **ready-for-dev (NON livrée)**
**Réf. plan :** `frontend-sprint-status.yaml` S13 · FR-A14
**Dépendances :** FE-025 (import Sage)
**Maître Scrum (frontend) :** MightyRaven

---

## Convention Git

- **Une story = une branche.** Branche : `fe-048`. Commits préfixés `FE-048`.
- Branche depuis `dev` **puis rebase sur `origin/dev`** ; PR vers `dev`.

---

## ⚠️ Bloquée par le backend

STORY-088 n'est **pas livrée** (backend `ready-for-dev`). Story prête à cadrer, mais son **contrat sera relevé à
la source** à la livraison — ne pas coder contre un contrat supposé. ⚠️ **Trou de contrat connu** : `mappingProfile` est aujourd'hui **accepté mais ignoré** par l'import Sage (relevé en FE-025). STORY-088 doit **le consommer** ET exposer le CRUD des profils. Ne pas livrer d'UI de mapping tant que le backend ne l'honore pas — sinon l'utilisateur paramètre dans le vide.

---

## Convention Maquette (préalable UI)

- **Maquette validée AVANT implémentation** : association colonne source → champ cible, CRUD de profils nommés.

---

## User Story

En tant que **comptable de cabinet aux imports récurrents**,
je veux **mémoriser l'association des colonnes de mon export et la réappliquer**,
afin de **ne pas reconfigurer le format à chaque import.**

---

## Contexte

Reprend le besoin de l'ancien FE-B03 mais côté **Atelier** (l'import a migré). Le mapping associe **colonne source → champ cible** (compte, libellé, débit, crédit) et se **mémorise** en profil réutilisable. **Dépend d'une évolution backend** (STORY-088) qui doit d'abord faire consommer `mappingProfile`.

---

## Périmètre

**Inclus**

- Écran de mapping colonne→champ, avec suggestion si l'entête est reconnu (proposer, pas décider).
- CRUD de **profils d'import** nommés, org-scopés.
- Application d'un profil à un import Sage (FE-025) via `mappingProfile`.
- Validation : compte + un montant doivent être mappés.

**Hors périmètre**

- L'import lui-même (FE-025).
- Tout mapping comptes→postes (Bilan, FE-030).

---

## Critères d'acceptation

1. Un mapping se compose et se **mémorise** en profil réutilisable.
2. Un profil s'applique à un import et le backend **le consomme réellement** (trou de contrat fermé).
3. La suggestion propose sans imposer (l'humain valide).
4. Types générés ; i18n FR ; tests unitaires + E2E.

---

## Definition of Done

- [ ] Contrat backend **relevé à la source** avant de coder (STORY-088 livrée).
- [ ] Tous les AC validés ; `lint`/`typecheck`/`test`/`build` verts.
- [ ] Zéro mock sur le chemin réel ; types générés depuis l'OpenAPI.
- [ ] Statut mis à jour dans les trackers.
- [ ] Commits sur `fe-048`, préfixés `FE-048`.

---

## Notes

- Créée le 2026-07-24. Reprend l'ancienne ligne `FE-A12` (renumérotée en format plat).
