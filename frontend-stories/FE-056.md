# Story FE-056 : Saisie assistée par le serveur — suggestion de compte pilotée par le référentiel (retrait du dictionnaire client)

Status: **done** (créée le 2026-07-31, livrée le 2026-08-07 — branche `fe-056`, commit `c1b777f`)

> ## ✅ Livraison — 2026-08-07
>
> **AC-1 → AC-7 tenus.** `plan-comptes.ts` et `plan-comptes.test.ts` sont supprimés ; `grep` ne rend plus
> aucune correspondance « libellé → compte » dans le dépôt. `api/suggest-comptes.ts` appelle
> `POST /balances/suggest-comptes` **en lot** (découpé à `MAX_LIBELLES_PAR_LOT = 200`) avec `{ libelles }`
> et rien d'autre. L'origine est rendue (`SURCHARGE`/`EXACT` en trait plein et teinte de succès,
> `APPROCHANT` en pointillé ambré, `AUCUN` sans pré-remplissage), `alternatives[]` devient un choix
> cliquable, et un échec de suggestion laisse la saisie entière.
>
> **AC-8 : `typecheck` · `lint` (0 warning) · `build` · 436 tests unitaires · 76 e2e — tous verts.**
> Les tests de `plan-comptes.test.ts` sont **remplacés**, pas supprimés : 7 cas de contrat dans
> `api/suggest-comptes.test.ts` (charge utile sans référentiel, lot unique, ordre préservé, découpage à
> la borne, `compte: null` intact, enveloppe, lot vide refusé), 10 cas d'écran et 4 e2e.
>
> ### ⚠️ Deux constats d'intégration, consignés dans le code
>
> **1. Les types de la surface `suggestion` sont écrits à la main — temporairement.**
> `src/types/api/balance.ts` date d'avant STORY-139. Le régénérer aujourd'hui n'apporterait pas que
> cette surface : il embarquerait **STORY-147** (contrat de balance à quatre colonnes), qui casse une
> quinzaine de fichiers et **appartient à FE-057**. Les formes sont donc copiées du DTO backend réel
> (`suggest-comptes.dto.ts`), avec l'ordre de les supprimer à la livraison de FE-057.
>
> **2. Le sélecteur de référentiel n'a pas pu disparaître — et ce n'était pas prévisible sur maquette.**
> `SubmitBalanceDto.referentiel` attend un **tag** (`SN` | `SMT` | `SFD-BCEAO`) ; l'enveloppe de
> suggestion renvoie un **code de paquet** (`syscohada-revise@2.1`). Le pont tag → code vit côté backend
> (`PONT_TAG`) et **n'a pas de réciproque exposée** : `GET /referentiels/actifs` type son objet
> `referentiel` en `Record<string, never>` dans l'OpenAPI (écart déjà relevé par FE-024). Déduire le tag
> côté client exigerait de **recoder la table que cette story retire**. Le sélecteur reste donc, mais il
> ne pilote plus aucune proposition, et l'écran affiche à côté le référentiel **réellement appliqué**.
> ⇒ **ticket backend ouvert.**
>
> ### ⛔ Integration Gate NON soldée
>
> Les 5 points du gate ci-dessous exigent le **stack docker** et une organisation **SFD-BCEAO** réelle.
> Le stack est arrêté et la régénération des types est bloquée par FE-057 : le gate se solde **avec
> elle**, pas avant. Rien n'a été maquillé en attendant — les tests ci-dessus sont sur stubs, et le
> disent.

**Epic :** FE-EPIC-007 — Atelier Balance — **retrofit de FE-026 (saisie manuelle)**
**Points :** 3 · **Sprint :** 7 (Atelier CORE — retrofit contrat) · **App :** `prospera-frontend-expert-comptable`
**API :** balance-service (:3007) — `POST /api/v1/balances/suggest-comptes` (préfixe logique `/atelier`, strip:true — cf. FE-024)
**Backend d'appui :** **STORY-139** (service de mapping « libellé libre → compte » piloté par le référentiel) — **LIVRÉE (done, 2026-07-29, PR #17 mergée)**
**Ticket d'origine :** `stories/TICKET-fe-026-retrait-dictionnaire-client.md` (ouvert par STORY-139, AC-6)
**Réf. plan :** `frontend-sprint-status.yaml` (S7 — amende FE-026)
**Backend prêt :** done ✅
**Dépendances :** **FE-026** (écran de saisie directe, livrée) · **FE-024** (routage `/atelier` → :3007)
**Maître Scrum (frontend) :** MightyRaven

---

## Convention Git

- **Une story = une branche.** Branche de travail : `fe-056`.
- **Commits préfixés `FE-056`** — ex. `FE-056 la suggestion de compte passe au serveur, piloté par le référentiel`.
- Branche depuis `dev` + **rebase sur `origin/dev` AVANT de coder** ; PR vers `dev`.
- ⚠️ **À faire après le merge de FE-027** (même zone `src/features/atelier/`).

---

## User Story

En tant que **comptable d'une organisation dont le référentiel n'est pas SYSCOHADA**,
je veux **que le compte proposé pendant ma saisie vienne du référentiel réellement actif pour mon organisation**,
afin que **la proposition soit juste** — aujourd'hui elle est calculée par un dictionnaire codé dans le navigateur,
qui ne connaît qu'un seul plan de comptes et **me propose des comptes faux**.

---

## Contexte

FE-026 a livré la saisie directe sur un modèle validé par le PO : « on saisit le **libellé** et le **montant**,
Prospera renseigne le **compte** ». Faute de service serveur à l'époque, la proposition est calculée par un
**dictionnaire codé côté client** — `src/features/atelier/config/plan-comptes.ts`, annoté « INTÉRIMAIRE » avec
renvoi explicite à STORY-139. Ce dictionnaire :

1. **duplique** une connaissance qui appartient aux paquets référentiels versionnés (STORY-056 SYSCOHADA,
   STORY-057 SFD-BCEAO) ;
2. est **mono-référentiel de fait** : « Charges de personnel » → `66` en SYSCOHADA révisé mais `64` en
   SFD-BCEAO ⇒ **une organisation de microfinance reçoit des propositions fausses** ;
3. ne connaît **aucune surcharge d'organisation** (`surcharges_rattachement`), alors que la règle écrite par le
   cabinet doit **primer** sur le plan ;
4. ne porte **aucune traçabilité** : impossible de dire sur quelle version de plan une proposition a été faite.

Le serveur restait seul juge du compte (validateur `^[0-9A-Za-z]{3,20}$` puis contrôle contre le plan), donc
**rien d'invalide n'est jamais entré en base** — mais l'aide à la saisie **induisait l'utilisateur en erreur**.

**STORY-139 (livrée) met le mapping à sa vraie place** : côté serveur, dérivé des paquets versionnés et des
surcharges d'organisation.

---

## Ce que le backend expose (STORY-139)

`POST /api/v1/balances/suggest-comptes` — gardé (`@RequiresBalanceAccess`, `TENANT_ADMIN`/`TENANT_USER`),
**batch**, **ordre d'entrée préservé**, chaque élément ré-émettant le `libelle` soumis :

- résolution **pilotée par le référentiel actif de l'organisation** — **aucun paramètre de référentiel à passer**
  (le front ne choisit pas, et ne doit pas prétendre choisir) ;
- **surcharge de l'organisation prioritaire** sur le plan ;
- `origine` ∈ `SURCHARGE | EXACT | APPROCHANT | AUCUN`, plus `score` et `motif` — de quoi afficher une
  proposition **forte** autrement qu'une proposition **approchée** ;
- `compte: null` quand rien ne correspond (« à préciser ») ;
- `alternatives[]` quand plusieurs comptes se disputent le libellé **sans départage possible** : c'est à
  l'utilisateur de trancher, **jamais au système** ;
- enveloppe : `referentiel { code, version }` + `checksum` + `stamp`.

Contrat complet dans le Swagger de `:3007` (`/api/docs`, tag `suggestion`).

---

## Portée

- **Supprimer** `src/features/atelier/config/plan-comptes.ts` **et toutes ses importations** — relevé au
  2026-07-31 : `src/features/atelier/api/submit-balance.ts` (type `ReferentielCode`),
  `src/features/atelier/components/saisie-directe-view.tsx`, et le test `plan-comptes.test.ts`.
  ⚠️ `ReferentielCode` doit venir du **type généré** de `:3007`, pas d'un type local ré-écrit ailleurs.
- **Brancher** la saisie sur `POST /balances/suggest-comptes` en **appel groupé** sur les libellés saisis —
  **pas un appel par ligne** (une saisie de 40 lignes ne doit pas produire 40 requêtes).
- **Rendre l'origine de la proposition** : distinguer visuellement `EXACT`/`SURCHARGE` (proposition **forte**)
  d'`APPROCHANT` (proposition **approchée**, à vérifier).
- **Rendre `AUCUN` explicite** — « à préciser » — plutôt que de pré-remplir un compte plausible.
- **Rendre `alternatives[]` comme un choix** quand il est présent.
- **Régénérer les types** de `:3007` si la surface `suggestion` n'y est pas encore (cf. **FE-057**, qui porte la
  régénération ; si FE-057 est déjà livrée, se contenter des types en place).

**Hors périmètre :** modifier le modèle de saisie par sections validé par le PO en FE-026 · la validation d'état
de la balance (trou backend `GAP-balance-validation-etat`, cf. FE-027) · l'UI de mapping d'import (FE-048) ·
toute proposition côté serveur qui déciderait à la place de l'utilisateur.

---

## Critères d'acceptation

1. **Plus aucun mapping `libellé → compte` codé dans le dépôt frontend** : `plan-comptes.ts` est supprimé, aucune
   importation ne subsiste, et aucun dictionnaire équivalent n'a été recréé ailleurs.
2. La saisie appelle `POST /balances/suggest-comptes` **en lot** sur les libellés saisis, et les propositions
   reviennent **dans l'ordre d'entrée** (le contrat le garantit — le prouver par test, c'est ce qui protège
   l'appariement ligne↔proposition).
3. `EXACT` et `SURCHARGE` sont **visuellement distincts** d'`APPROCHANT` ; aucune proposition approchée n'est
   présentée comme certaine.
4. `AUCUN` / `compte: null` affiche « à préciser » et **ne pré-remplit rien**.
5. `alternatives[]` est rendu comme un **choix explicite** ; aucun départage automatique n'est fait côté client.
6. Le front **ne passe jamais** de code de référentiel à la route (la résolution appartient au serveur) — prouvé
   par un test sur la charge utile envoyée.
7. Une réponse en erreur (403 gate, 400, réseau) **ne casse pas la saisie** : la ligne reste saisissable à la
   main, comme aujourd'hui — la suggestion est une **assistance**, jamais un prérequis (invariant DO-1).
8. `typecheck`, `lint`, `build`, tests unitaires et E2E **verts** ; les tests de `plan-comptes.test.ts` sont
   remplacés par des tests de la nouvelle couche (pas simplement supprimés).

---

## Integration Gate (DoD étendue)

1. **Vérification en vrai navigateur** contre le stack docker, sur une organisation **SFD-BCEAO** — c'est
   précisément le cas que le dictionnaire client traitait **faux**. Prouver que « Charges de personnel » propose
   le compte du référentiel **de cette organisation** et non celui de SYSCOHADA.
2. Même vérification sur une organisation **SYSCOHADA** (non-régression du cas nominal de FE-026).
3. Cas **surcharge d'organisation** confronté en réel : une règle écrite par le cabinet **prime** sur le plan et
   remonte en `origine: SURCHARGE`.
4. Cas `alternatives[]` confronté en réel : l'écran fait trancher l'humain.
5. **Zéro mock** sur le chemin vérifié ; écart résiduel tracé en ticket backend.

---

## Notes

- ⚠️ **Le front n'était pas cassé, il était faux hors SYSCOHADA.** Priorité Should — mais la dette grandit à
  chaque écran qui s'appuiera sur la saisie, et le vertical IMF (SFD-BCEAO) est au backlog.
- Le ticket d'origine (`stories/TICKET-fe-026-retrait-dictionnaire-client.md`) a été écrit côté backend parce que
  le dépôt frontend **n'était pas dans le workspace** au moment de STORY-139 ; il l'est. Cette story **est** la
  réponse au ticket — le fermer à la livraison.
- Le serveur reste **seul juge** du compte à la persistance (validateur + contrôle contre le plan) : cette story
  ne déplace pas l'autorité, elle rend l'**assistance** aussi juste que l'autorité.

---

## Amendement FE-063 (2026-08-19) — cet écran s'exerce DANS UN DOSSIER

Cette fiche a été écrite quand `balance-service` dérivait sa portée du **jeton** :
« aucune route ne prend d'`orgId` : l'organisation vient toujours du JWT ». Ce
n'est plus vrai. STORY-236 a déplacé toute la donnée de dossier sous
`/dossiers/:dossierId/…`, et FE-063 a re-scopé les appels correspondants.

**Ce qui change pour la lecture de cette fiche :**

- Partout où elle dit « les balances **du cabinet** » / « de l'organisation », lire
  **« du dossier ouvert »**. L'organisation reste la frontière de sécurité ; elle
  n'est plus l'unité de travail.
- Les chemins cités (`GET /balances`, `POST /balances`, `POST /balance/import/sage`,
  `POST /balances/:id/valider|rejeter`) sont **périmés** : ils vivent désormais
  sous `/dossiers/:dossierId/…`. ⚠️ Ne pas les recopier — ils rendraient un `404`
  que rien n'expliquerait.
- Un chemin de donnée de dossier **ne s'écrit plus à la main** : il passe par
  `cheminDossier()` (`src/features/atelier/api/paths.ts`). Une garde
  (`scope-dossier.guard.test.ts`) fait échouer la suite sinon.
- **Deux exceptions, et elles sont volontaires** : `GET /referentiels/actifs` et
  `POST /balances/suggest-comptes` restent de **niveau organisation** — le
  paramétrage et le plan comptable appartiennent au cabinet, pas au client. Les
  scoper rendrait `404`.
- Deux refus nouveaux, à distinguer des refus métier : **404 `DOSSIER_INTROUVABLE`**
  (dossier inexistant ou d'une autre organisation — jamais 403, anti-énumération)
  et **409 `DOSSIER_ARCHIVE`** (écritures refusées, lectures autorisées, D9). Ils
  se lisent avec `lireRefusDossier` (`lib/dossier-refus.ts`), **avant** tout
  lecteur de refus métier — `lireRefusBalance` ne les connaît pas.

⚠️ **Propre à cette story** : `POST /balances/suggest-comptes` est **restée de
niveau organisation**, et c'est le bon choix — le plan comptable qu'elle
interroge est celui du cabinet, pas du client ouvert. C'est la **seule** route de
l'Atelier dont le chemin commence par `balances` sans être une donnée de dossier ;
elle figure explicitement dans l'allowlist de `paths.ts` plutôt qu'en exception
tacite, et une contre-épreuve vérifie qu'on ne la scope pas par excès de zèle.
