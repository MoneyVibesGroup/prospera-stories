# Story AP-10 : Organisations d'un module — qui l'utilise, et dans quelle version

Status: review (implémentée le 2026-08-09, branche `ap-10`)

**Epic :** AP-EPIC-002 — Entitlements & catalogue
**Points :** 3 · **Sprint :** 10 · **App :** `frontend-admin-panel`
**API :** admin-panel BFF (`/admin/modules/:code/organizations`, `/admin/modules/:code/summary`) → platform-catalog-service · **Backend d'appui :** **STORY-142** (index inverse), **STORY-143** (proxy BFF + résolution des noms)
**Réf. plan :** `frontend-sprint-plan-admin-panel-2026-07-12.md` · `frontend-sprint-status.yaml`
**Backend prêt :** ✅ done
**Dépendances :** AP-04 (catalogue), AP-05 (entitlements — vue org-centrée)
**Maître Scrum (frontend) :** MightyRaven

---

## ⚠️ Deux corrections à cette story, faites contre le code livré (2026-08-09)

**1. La permission est `org:read`, PAS `catalog:read`** (le critère nº 7 ci-dessous était faux).
`AdminModulesController` est gardé par `org:read`, et l'amont `platform-catalog-service` aussi — son
en-tête l'explique : la donnée renvoyée EST de la donnée d'organisation, et la garder plus faiblement
rouvrirait sur elle **une seconde porte plus basse**. Mesuré en vérification docker : un jeton
`catalog:read` seul franchissait le panel puis se faisait refuser en 403 par le catalogue, donc la
garde du BFF était décorative. La console s'aligne sur l'autorité.

**2. L'en-tête « BLOQUÉE » était périmé de six semaines.** STORY-142 et STORY-143 sont livrées depuis
le 2026-07-31 ; le tracker le disait, ce fichier non. Conservé ci-dessous pour l'historique :

> Aucune route n'existe pour lire les entitlements **par module**. Vérifié sur `origin/dev` de
> `prospera-platform-catalog-service` (`89d6eb9`) : la collection n'a d'index que
> `{organizationId, moduleCode}` et `{organizationId}` — interroger par `moduleCode` seul provoquerait
> un collection scan, et **aucun endpoint ne l'expose**. Le BFF, lui, ne proxifie **aucune** lecture
> d'entitlement (`origin/dev` de `prospera-admin-panel-service`, `0521258`).

---

## Convention Git

- **Une story = une branche.** Branche : `ap-10`. Commits préfixés `AP-10`.

---

## Convention Maquette (préalable UI)

- **Maquette validée AVANT implémentation** (liste des organisations d'un module + répartition par
  version), conforme au Design System Prospera, publiée en Artifact.
- **Ne pas coder tant que la maquette n'est pas validée.**

---

## User Story

En tant qu'**administrateur plateforme Money Vibes**,
je veux **ouvrir un module du catalogue et voir quelles organisations l'utilisent, dans quelle version**,
afin de **mesurer l'impact d'une dépréciation avant de la décider**.

---

## Contexte

**Le trou que cette story bouche.** AP-04 affiche déjà, pour chaque module, une fenêtre de support
N/N-1 et un nombre d'organisations. Mais au moment de faire sortir une majeure du support, l'admin
n'a aucun moyen de savoir **qui** est encore dessus. Il décide donc à l'aveugle sur le seul geste
d'AP-04 qui soit irréversible pour les clients.

**Le point d'entrée retenu : module → organisations.** C'est la demande du PO, et c'est aussi le seul
sens qui ne duplique rien :

| Sens | Qui le porte |
|---|---|
| **org → modules/versions** (fiche d'une organisation) | **AP-05**, critère d'acceptation nº 1 — déjà écrit, ne pas redonder |
| **module → organisations/versions** | **cette story** — n'existe nulle part |

Les deux vues lisent la même collection `Entitlement` par deux entrées différentes. AP-10 n'ajoute
donc **aucun concept** : elle ajoute une lecture.

---

## Périmètre

**Inclus :**
- Depuis le tiroir d'un module (AP-04), une section **« Organisations »** : liste paginée avec nom de
  l'organisation, **version utilisée**, référentiel le cas échéant, statut de l'entitlement
  (`ACTIVE` / `SUSPENDED` / `REVOKED`), date de dernière modification.
- **Répartition par version** en tête de section (`/summary`) : combien d'organisations sur v1.x,
  combien sur v2.x — c'est ce qui rend la fenêtre N/N-1 d'AP-04 lisible en conséquences.
- **Filtre par statut** ; par défaut **tout est affiché**, révocations comprises (une révocation
  récente est le signal intéressant).
- Depuis une ligne : **lien vers la fiche organisation** (AP-02).
- **Source dégradée** : si les noms d'organisation ne sont pas résolus (`organizationSource: "degraded"`,
  patron STORY-047), afficher les identifiants et le dire — jamais un écran vide ni une erreur.

**Hors périmètre :**
- Octroyer / révoquer depuis cette vue → **AP-05** (le geste d'écriture a son écran).
- La liste des modules d'une organisation → **AP-05**.
- Le nombre d'organisations affiché dans la table AP-04 (déjà là).

---

## Critères d'acceptation

- [ ] Le tiroir d'un module affiche la liste paginée des organisations qui l'utilisent, avec la **version** de chacune.
- [ ] La répartition par version est affichée et **concorde** avec le total de la liste.
- [ ] Le filtre par statut fonctionne ; sans filtre, les entitlements `REVOKED` sont visibles et distingués.
- [ ] Un module que personne n'utilise affiche un état vide explicite (« aucune organisation »), pas un tableau vide.
- [ ] Un clic sur une ligne mène à la fiche organisation (AP-02).
- [ ] `organizationSource: "degraded"` → identifiants affichés + bandeau expliquant que les noms sont indisponibles.
- [x] ~~Un acteur sans `catalog:read`~~ **sans `org:read`** ne voit pas la section (masquage `perms[]`,
      patron AP-08) — et la section n'émet alors **aucune requête** vers l'amont.
- [x] i18n FR ; tests : rendu liste, répartition, filtre, état vide, source dégradée.

---

## Integration Gate (DoD étendue)

1. [x] Types **générés** depuis l'OpenAPI réel du BFF (`npm run gen:api` contre le stack docker).
2. [x] Contrat confronté au vrai endpoint : `pageSize` **25 par défaut, plafonné à 100** ; `status`
       hors énum → **400** ; paramètre inconnu → **400** (`forbidNonWhitelisted`) ; module inconnu →
       **404** ; sans jeton → **401**.
3. [x] **Zéro mock** sur le chemin réel — deux tests ajoutés à `e2e/integration-gate.spec.ts`,
       exécutés en navigateur contre le stack : le tiroir liste de vraies organisations **avec leur
       raison sociale** (donc la jointure BFF → auth-service a bien eu lieu), et le filtre de statut
       part sur le réseau au lieu de trier localement.
4. [x] Écart tracé : **`TICKET-BACKEND-repartition-par-version-non-croisee-avec-le-statut.md`**.

---

## ⚡ Ce que l'Integration Gate a trouvé, et que les tests unitaires ne pouvaient pas trouver

Sur le stack réel, `balance` affiche **7 organisations dont 6 révoquées**. `byVersion` et `byStatus`
sont deux ventilations du même ensemble, **jamais croisées** : « combien d'organisations encore
actives sur la v1.0 ? » n'est servi par personne.

L'erreur va dans le sens le plus coûteux — elle fait **renoncer à déprécier** une version que plus
personne n'utilise. Non rattrapable côté client (le déduire de la page affichée donnerait un compte
juste sur un périmètre faux). L'écran nomme donc son périmètre — « tous statuts confondus,
révocations comprises · 1 est encore actif » — en attendant le contrat.

---

## Definition of Done

- [x] Critères d'acceptation validés ; tests verts (533 unitaires, dont 17 propres à AP-10).
- [x] `lint` / `typecheck` / `test` / `build` verts.
- [ ] Statut mis à jour dans les trackers (**après signal du PO**).
- [x] Commits sur `ap-10`, préfixés `AP-10`.
