# Story FE-014 : Accès modules & entitlements (home des modules + shell config-driven)

Status: ready-for-dev

**Epic :** FE-EPIC-006 — Accès modules & entitlements
**Points :** 5 · **Sprint :** 3 (programme) · **App :** `prospera-frontend-expert-comptable`
**API :** expert-comptable (`/ec/tenant`, `/ec/modules`) + platform-catalog-service (`/catalog`) via gateway · **Backend d'appui :** STORY-014 (TenantStateGuard/`/tenant/state`), STORY-033/034 (entitlements + `entitlement.changed`)
**Réf. plan :** `docs/frontend-sprint-status.yaml` · `docs/frontend-sprint-plan-expert-comptable-2026-07-08.md` (addendum v2)
**Backend prêt :** S7 (catalog) — le gate KYC/abonnement est déjà servi par `/tenant/state` (livré)
**Dépendances :** FE-002 (client API + mapping 403), FE-003 (shell/nav), FE-010 (état tenant/onboarding)
**Maître Scrum (frontend) :** MightyRaven

---

## Convention Git

- **Une story = une branche.** Branche de travail : `fe-014`.
- **Commits préfixés `FE-014`**, puis le commentaire — ex. `FE-014 home des modules pilotée par entitlements`.
- Ne pas travailler sur `dev`/`main` : brancher `fe-014` depuis `origin/dev` à jour, PR vers `dev` une fois en `review`.

---

## Convention Maquette (préalable UI)

- **Maquette validée AVANT implémentation** (home des modules + états verrouillé/déverrouillé), conforme au Design System Prospera (`docs/prospera-design-system.md`), livrée dans `scratchpad/` puis publiée en Artifact.
- **Ne pas coder tant que la maquette n'est pas validée.**

---

## User Story

En tant qu'**utilisateur d'un cabinet (TENANT_ADMIN / TENANT_USER)**,
je veux **voir sur ma page d'accueil les modules qui me sont réellement ouverts et l'état de ceux qui ne le sont pas**,
afin de **savoir immédiatement ce que je peux utiliser (ex. Bilan) et ce qui me manque pour débloquer le reste**.

---

## Contexte

Cette story pose le **socle config-driven** de l'app cliente : l'app n'affiche que les **modules `ACTIVE`** pour l'organisation, en lisant l'**entitlement** (read-model alimenté par `entitlement.changed` du `platform-catalog-service`, octroyé depuis l'**admin-panel**). C'est le pendant client de la tour de contrôle : *l'admin allume un module → il apparaît ici*.

Le gate d'accès effectif combine trois conditions (déjà exposées côté backend) : `emailVerified` + KYC `APPROVED` + entitlement `ACTIVE` (l'abonnement viendra avec le Module 2). Le frontend **reflète** cet état et **oriente** — il ne décide jamais de l'autorisation (source de vérité = backend, 403 explicites).

> Prépare l'arrivée du module **Bilan** (FE-028+) et de tout futur module/vertical : ajouter un module = le déclarer + le brancher, l'affichage suit l'entitlement.

---

## Périmètre

**Inclus :**
- **Registre de modules côté client** (config déclarative : clé, libellé, icône, route, entitlement requis) — extensible par vertical via `NEXT_PUBLIC_VERTICAL`.
- **Home des modules** : grille de cartes, chaque module `ACTIVE` = accessible (CTA d'entrée), sinon **verrouillé** avec la raison (`KYC_NOT_APPROVED`, `SUBSCRIPTION_REQUIRED`, entitlement absent) + CTA vers l'étape à traiter.
- **Lecture de l'état** : `GET /ec/tenant` (email/KYC/abonnement, déjà en FE-010) + **entitlements** de l'org (via `/ec/modules` ou `/catalog` — voir Notes) ; TanStack Query.
- **Navigation config-driven** : la nav latérale (FE-003) ne montre que les modules ouverts ; un module verrouillé n'apparaît pas (ou grisé selon la maquette).
- **Cohérence 403** : réutilise `accessErrorTarget`/`useAccessErrorRedirect` (FE-010) pour orienter quand un module gardé renvoie 403.

**Hors périmètre :**
- Le contenu des modules eux-mêmes (Bilan = FE-028+).
- L'octroi/révocation d'entitlement → **admin-panel** (AP-05).
- Paiement/abonnement (FE-011/012/013).

---

## Critères d'acceptation

- [ ] Un registre de modules déclaratif existe ; l'ensemble affiché dérive de la config du vertical courant (aucune liste en dur dans les composants d'UI).
- [ ] La home liste les modules ; un module dont l'entitlement est `ACTIVE` (et pré-requis KYC OK) est **ouvert** (CTA d'entrée fonctionnel).
- [ ] Un module non ouvert est **verrouillé** avec la **raison** affichée (KYC non approuvé / abonnement requis / non attribué) + CTA contextuel (vers `/kyc`, onboarding, ou message « contactez votre administrateur »).
- [ ] La navigation ne propose que les modules ouverts ; l'accès direct à une route de module non ouvert redirige/oriente (pas d'erreur brute).
- [ ] L'état est rafraîchi à l'entrée de l'espace `(app)` et après les actions clés (approbation KYC, changement d'entitlement) ; cohérence éventuelle tolérée (staleTime + invalidation).
- [ ] i18n FR ; tests : mapping (état + entitlements) → modules ouverts/verrouillés + raisons ; cas 403 orienté.

---

## Notes techniques

| Composant | Fichier (proposé) | Nature |
|---|---|---|
| Registre modules | `src/features/modules/config/registry.ts` (clé, route, entitlement, vertical) | Nouveau |
| Query entitlements | `src/features/modules/api/get-modules.ts` + `hooks/useModules.ts` | Nouveau |
| Home | `src/features/modules/components/ModulesHome.tsx` + `ModuleCard.tsx` | Nouveau |
| Nav filtrée | maj `src/components/layout/nav-config.ts` (filtrage par modules ouverts) | Modif |

**Décisions & vigilance :**
- **Contrat entitlement** : si `expert-comptable` agrège déjà l'entitlement dans `/tenant/state`, préférer une source unique (`/ec/tenant` étendu) ; sinon lire `/catalog` (réconciliation org). **À confirmer à la génération OpenAPI** (une seule source d'état effectif visée). Documenter le contrat local en attendant.
- **Affichage ≠ autorisation** : un module masqué reste protégé côté backend ; le masquage est ergonomique, pas sécuritaire.
- **Extensibilité vertical** : le registre doit permettre `NEXT_PUBLIC_VERTICAL=cabinet|imf|…` → jeu de modules différent sans toucher au code d'UI.

---

## Tasks / Subtasks

- [ ] Registre de modules déclaratif + résolution par vertical (AC 1)
- [ ] Query état + entitlements + hook (AC 5)
- [ ] `ModulesHome` + `ModuleCard` (ouvert / verrouillé + raison) (AC 2, 3)
- [ ] Filtrage de la navigation + garde de route module (AC 4)
- [ ] Orientation 403 (réutilise FE-010) (AC 3)
- [ ] i18n + tests (AC 6)

---

## Definition of Done

- [ ] Tous les critères d'acceptation validés ; tests verts.
- [ ] Aucune décision d'autorisation côté client (reflet d'état uniquement).
- [ ] `lint` / `typecheck` / `test` / `build` verts (local + CI).
- [ ] Statut mis à jour dans `docs/frontend-sprint-status.yaml` (+ tracker app).
- [ ] Commits sur `fe-014`, préfixés `FE-014`.

---

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
