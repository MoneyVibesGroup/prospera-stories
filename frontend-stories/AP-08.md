# Story AP-08 : RBAC plateforme — membres, rôles composables, masquage d'UI sur `perms[]`

Status: review

**Epic :** AP-EPIC-000 — Socle admin & sécurité
**Points :** 8 · **Sprint :** 6 · **App :** `prospera-admin-panel`
**API :** admin-panel BFF (`/admin`, :3010) → proxy vers auth-service (:3001) · préfixe global **`/api/v1`**
**Backend d'appui :** STORY-103 (fondation RBAC), STORY-104 (CRUD rôles + catalogue + invitation org-less), STORY-105 (migration des amont), **STORY-106 (proxy BFF — ✅ LIVRÉE)**
**Réf. plan :** `frontend-sprint-status.yaml` v3.0.0 (S6) · `sprint-status.yaml` sprint 10, D15/EPIC-025
**Backend prêt :** ✅ **done** — 103/104/105 le 2026-07-16, **106 le 2026-07-17** (🏁 EPIC-025 clos)
**Maître Scrum (frontend) :** MightyRaven

---

## ✅ Statut : DÉBLOQUÉE (2026-07-28)

**Le pari est gagné : STORY-106 est livrée.** Cette story était marquée `blocked` depuis le
2026-07-17 en pariant sur sa sortie — le pari a été tenu le jour même, et le statut n'a jamais été
réancré. Elle est restée bloquée sur une phrase, pas sur la réalité, pendant onze jours.

**Vérification faite** (celle-là même que la story prescrivait), vérifié le 2026-07-28 sur `origin/dev` de `prospera-admin-panel-service` (commit `0521258`) :

- `origin/dev` est à `0521258` (MNV-107), plus à `52c6f23` (MNV-049) ;
- `grep -r RequirePermissions src/` **renvoie des résultats** — il était vide ;
- les six routes déclarées manquantes **existent** :
  `GET /admin/permissions` · `GET|POST /admin/roles` · `PATCH|DELETE /admin/roles/:name` ·
  `PATCH /admin/users/:id` ;
- le gating par permission est en place : `@RequirePermissions(Permission.ROLE_MANAGE)` et
  `@RequirePermissions(Permission.USER_INVITE)`.

Les deux conséquences redoutées sont donc levées : le front a bien un chemin vers la surface de
gestion, et le gating n'est pas cosmétique puisque le BFF applique lui-même la permission.

⚡ **Cette story porte les rôles internes Comptable / Marketing / DG** demandés par le PO le
2026-07-28. Aucune story frontend supplémentaire n'a été créée pour eux : la mécanique est ici.
Son AC nº 3 passera de **8 à 12 permissions** après STORY-140, et trois rôles système seedés
supplémentaires apparaîtront en lecture seule.

---

## Convention Git

- **Une story = une branche.** Branche de travail : `ap-08`.
- **Commits préfixés `AP-08`** — ex. `AP-08 gestion des membres plateforme + composition de rôles`.
- PR vers `dev` du dépôt `prospera-admin-panel`.

---

## Convention Maquette (préalable UI)

- **Maquette validée AVANT implémentation** — 3 écrans : liste des membres plateforme, dialogue
  d'invitation + attribution de rôle, éditeur de rôle (cases à cocher sur le catalogue de permissions).
- Conforme au Design System Prospera (`prospera-design-system.md`), publiée en Artifact.
- **Ne pas coder tant que la maquette n'est pas validée.**

---

## User Story

En tant qu'**administrateur plateforme Money Vibes**,
je veux **inviter un opérateur, lui attribuer un rôle, et composer un rôle à partir du catalogue de permissions**,
afin de **déléguer une partie de l'administration (revue KYC, support, audit) sans donner les pleins pouvoirs**.

---

## Contexte

**Ce que le backend a livré et que personne n'a encore branché.** Le 2026-07-16, le backend a livré
D15/EPIC-025 (STORY-103/104/105) : une refonte complète du RBAC plateforme. **Aucune story frontend ne
la couvrait** — alors que STORY-104 a été conçue *pour* un panel : sa note de clôture dit explicitement
que le catalogue expose les 8 permissions **avec leurs libellés** parce que « le panel 106 a besoin des
libellés, pas d'un `string[]` nu ».

**Le modèle, à respecter scrupuleusement côté UI** (ligne de partage posée par D15) :

- **PERMISSIONS = code.** Catalogue **figé** de 8 permissions. Une permission n'existe que si un guard
  la vérifie. L'UI ne peut **jamais** en inventer une : elle lit `GET /admin/permissions`.
- **RÔLES = données.** Un rôle n'est qu'un **nom donné à un paquet de permissions**, composable à chaud.
  Les personas (`KYC_OFFICER`, `SUPPORT`, `AUDITOR`) ne sont **pas** des valeurs d'enum : ce sont des
  **rôles système seedés** dans la collection `roles`, marqués `isSystem: true` et **non supprimables**.
- **Le JWT porte `perms[]` EN PLUS de `roles[]`** (additif). **TTL access = 15 min** ⇒ un changement de
  rôle met **jusqu'à 15 min** à se propager. L'UI doit le dire à l'admin qui vient de modifier un rôle,
  sinon il croira à un bug.

**Cette story amende AP-01**, dont le volet « RBAC strict PLATFORM_ADMIN » (tout-ou-rien) est **périmé**.

---

## Portée

### 1. Membres plateforme
- **Liste** (`GET /admin/users` via proxy BFF) : e-mail, rôle plateforme, statut (`ACTIVE` / `INVITED`).
- **Invitation org-less** (`POST /admin/users`) : e-mail + rôle. ⚡ C'est une invitation **sans
  organisation** — le paradoxe que STORY-104 a corrigé (un `PLATFORM_ADMIN` a `tenantId: null` et ne
  pouvait donc pas inviter). Ne pas réutiliser le composant d'invitation **client** (FE-008) tel quel :
  le flux client est org-scopé et partage `InvitationService` côté backend, mais **la surface HTTP diffère**.
- **Réattribution de rôle** (`PATCH /admin/users/:id`). ⚠️ **404 sur un compte sans `platformRole`** :
  cette route **redistribue** un rôle entre opérateurs, elle ne **promeut pas** un utilisateur tenant
  (anti-énumération). L'UI ne doit donc jamais proposer « promouvoir ce membre du cabinet ».

### 2. Catalogue & rôles
- **Catalogue** (`GET /admin/permissions`) → 8 entrées `{ value, description }`. Afficher **le libellé**,
  jamais le `value` brut.
- **Rôles** (`GET|POST /admin/roles`, `PATCH|DELETE /admin/roles/:name`) : composition par cases à cocher
  sur le catalogue.
- **Rôles système** (`isSystem: true`) : lecture seule, **non supprimables**, visuellement distingués.
  `isSystem` envoyé dans un POST → **400** (le backend refuse ; l'UI ne doit pas l'exposer).

### 3. Masquage d'UI sur `perms[]` (amende AP-01)
- Le shell admin lit `perms[]` du JWT et masque les entrées de nav + actions non permises.
- **Le masquage est une commodité, PAS une sécurité** : l'autorité reste les guards amont (105) + BFF (106).
- Migrer le gating d'AP-01/02/03/04/05 : `AdminOrgsController` (lecture) → `org:read` ;
  actions → `kyc:approve` / `kyc:reject` / `entitlement:grant` / `entitlement:revoke`.

---

## Garde-fous backend à REFLÉTER dans l'UI

Ces règles sont **déjà appliquées côté backend** (et prouvées en docker). L'UI doit les rendre
compréhensibles **avant** que l'admin se prenne un 403 opaque :

| Garde-fou | Comportement backend | Attendu UI |
|---|---|---|
| **Anti god-mode** | `assertCanGrant` : on ne peut pas octroyer une permission qu'on ne détient pas soi-même → **403 générique** (ne divulgue pas quelle permission manque) | Ne proposer à la composition **que** les permissions présentes dans le `perms[]` de l'acteur |
| **Anti-lockout** | `assertNotLastPlatformAdmin`, **sous la session de la mutation** (le TOCTOU de la revue de 103 est fermé) | Désactiver l'action sur le dernier `PLATFORM_ADMIN` + expliquer pourquoi |
| **Rôle référencé** | Suppression refusée si le rôle a des porteurs — **y compris `INVITED`** (2ᵉ compteur `countAssignedByPlatformRole`, sans filtre de statut) | Message explicite : « N porteur(s), dont des invités en attente » |
| **`name` non éditable** | C'est la clé fonctionnelle recopiée dans `User.platformRole` ; le renommer ferait pointer les porteurs vers un rôle disparu, et `permissionsFor()` dégrade un rôle inconnu en `[]` **sans erreur** = perte totale des droits, en silence | Pas de champ « renommer ». Proposer le chemin sûr : **créer + réattribuer + supprimer** |
| **Propagation** | TTL access 15 min | Après modification d'un rôle : « effectif d'ici 15 min pour les sessions ouvertes » |

---

## Critères d'acceptation

1. La liste des membres plateforme affiche e-mail, rôle et statut (`ACTIVE` / `INVITED`).
2. Inviter un opérateur (e-mail + rôle) → il apparaît en `INVITED` ; l'invitation part (Mailhog en dev).
3. Le catalogue affiche les **8 permissions avec leurs libellés** (jamais le `value` brut).
4. Créer un rôle non-système en cochant des permissions → il apparaît dans la liste, `isSystem: false`.
5. Les rôles système sont en lecture seule et leur suppression est impossible (action désactivée + explication).
6. Supprimer un rôle porté par un membre **`INVITED`** est refusé, avec un message qui dit pourquoi.
7. **Anti god-mode** : un acteur `{role:manage, org:read}` ne se voit proposer que `org:read` à la
   composition ; s'il force `entitlement:grant`, il prend un 403 et l'UI l'affiche sans fuite d'info.
8. **Anti-lockout** : retirer son rôle au dernier `PLATFORM_ADMIN` est refusé et expliqué.
9. Aucun champ « renommer un rôle » n'existe ; le parcours créer/réattribuer/supprimer est proposé.
10. Le shell masque les entrées de nav non permises par `perms[]`, **et** AP-02/03/04/05 gatent leurs
    actions sur la permission correspondante.
11. **VÉRIF DOCKER (le test qui prouve la délégation)** : un `PLATFORM_KYC_OFFICER` approuve un KYC
    → **200** ; le même tente un grant d'entitlement → **403**. C'est l'assertion attendue par STORY-106.
12. Le contrat consommé est **généré depuis l'OpenAPI** (`/api/docs-json` de l'IdP), pas écrit à la main
    (règle Integration Gate). ⚠️ STORY-104 a corrigé une **fuite de champs internes Mongo** (`_id`, `__v`,
    `createdAt`, `updatedAt`) dans les réponses `/admin/roles` — invisible en unitaire, trouvée en docker.
    Régénérer les types **après** 106 : ne pas figer un contrat sur une réponse polluée.

---

---

## 📋 Note de clôture (2026-08-09) — livrée sur la branche `ap-08`

### Ce que la story annonçait, et ce que le backend sert réellement

**La vérification a précédé le code**, contre le stack docker (BFF :3010 → IdP :3001).
Trois écarts, tous en faveur du backend :

| La story disait | La réalité mesurée |
|---|---|
| catalogue de **8** permissions (12 après STORY-140) | **13** — `referentiel:publish` s'est ajouté |
| 3 rôles système (`KYC_OFFICER`, `SUPPORT`, `AUDITOR`) | **7** — Comptable / Marketing / DG sont **déjà seedés** |
| AC 7 : « un acteur `{role:manage, org:read}` ne se voit proposer que `org:read` » | `assertCanGrant` = cible ⊆ détenu ⇒ il doit voir **les deux** |

⚡ **Conséquence pour l'AC nº 3 : il n'a pas eu à être corrigé, et c'est le point de
conception de cette story.** L'écran **lit** le catalogue, il ne le recopie pas : il affiche
ce que `GET /admin/permissions` sert, groupé par domaine, avec un groupe « autres » qui
recueille toute permission inconnue de ses domaines. Le jour où l'IdP en ajoute une 14ᵉ,
elle apparaît sans qu'une ligne de front change. Un test le prouve
(`permissions.test.ts` — « montre une permission INCONNUE au lieu de l'avaler »).

⚠️ **L'AC nº 7 est implémenté selon le garde-fou, pas selon sa formulation.** Le composeur
propose l'intersection `catalogue ∩ perms[]` de l'acteur — pour `{role:manage, org:read}`,
donc les deux. La garantie testée est celle qui compte : *aucune permission hors du `perms[]`
de l'acteur n'est proposée*, et un 403 forcé est rendu sans fuite d'information.

### Garde-fous confirmés en live (et non supposés)

| Geste | Réponse réelle |
|---|---|
| `PATCH` / `DELETE` d'un rôle système | **403** |
| `isSystem` dans un `POST /admin/roles` | **400** — `property isSystem should not exist` |
| `name` dans un `PATCH /admin/roles/:name` | **400** — `property name should not exist` |
| `DELETE` d'un rôle porté par un **INVITED** | **409** |
| `PATCH` du dernier `PLATFORM_ADMIN` actif | **409** |
| `PATCH` d'un compte sans `platformRole` | **404** (anti-énumération) |
| invitation org-less | **201**, e-mail reçu dans Mailhog |
| TTL du jeton d'accès | **900 s** — les « 15 min » annoncées à l'écran sont exactes |

⚠️ Les 403 et 409 de l'IdP sont **génériques par conception** (« Action non autorisée. »).
Le sens est donc reconstitué du **code + geste** dans `rbac-errors.ts` : le même 409 signifie
« anti-lockout » sur une réattribution et « rôle encore porté » sur une suppression.
Les confondre enverrait l'opérateur chercher au mauvais endroit.

### Deux écarts de contrat à corriger côté backend

1. **Le Swagger du BFF ne déclare aucun `type:` de réponse.** Ses `@ApiOkResponse({ description })`
   n'ont pas de schéma : `openapi-typescript` produit `content?: never` pour les six routes de
   cette story. **Aucun consommateur ne peut se typer sur le BFF.** Contournement appliqué :
   les types viennent de la spec de l'**IdP** (`PlatformRoleDto`, `PermissionCatalogItemDto`,
   `PaginatedUsersDto`), qui les décrit correctement — le BFF traversant fidèlement le corps
   amont, ces schémas décrivent bien ce que la console reçoit. L'AC nº 12 est donc tenu
   (types **générés**, non écrits à la main), mais par le bon des deux fichiers.
2. **`POST /admin/users` (201) omet `firstName` / `lastName`** alors que `UserResponseDto` les
   déclare **requis**. L'écran n'affiche donc rien depuis ce corps : il recharge la liste.
   Un test du gate **constate** l'écart au lieu de le contourner en silence.

### Ce qui a été livré

- `src/features/rbac/` — types (générés), `permissions.ts` (groupement, anti god-mode,
  comptage des porteurs), `api/rbac-client.ts` (6 routes), `rbac-errors.ts`, `hooks/useRbac.ts`.
- 7 composants : liste des membres, invitation org-less, changement de rôle, grille de rôles,
  éditeur (cases à cocher sur le catalogue), suppression, encarts de refus.
- `/acces` + entrée de nav, **section i18n `rbac` complète** (aucun texte en dur).
- `components/ui/checkbox.tsx` — le socle n'en avait pas ; aucun écran n'avait eu besoin d'un
  choix multiple avant la composition d'un rôle.
- **Masquage migré sur `perms[]`** : nav (`visibleNavItems`), KYC (`kyc:approve` **et**
  `kyc:reject`, séparément — l'issue consolidée décide de la permission requise), entitlements
  (`grant` / `revoke` séparément), catalogue (`catalog:manage` vs `referentiel:publish`).
- Maquette `docs/mockups/ap-08-rbac-plateforme.html`, **dérivée d'AP-12** (socle CSS extrait,
  non recopié) et publiée en Artifact.

### Deux décisions de conception, validées par le PO

1. **L'anti-lockout est le seul grisage assumé de l'écran.** Partout ailleurs une action non
   permise **disparaît** (règle d'AP-20). Ici elle reste, désactivée : la cause est un **état**,
   pas un droit manquant. La faire disparaître ferait chercher la permission à cocher pour la
   rendre. L'explication est donnée une fois, sous le tableau.
2. **Aucun champ « renommer »**, remplacé par l'encart « le chemin sûr » (créer → réattribuer →
   supprimer) et un bouton « composer un rôle jumeau ». Le 400 de l'IdP le confirme.

### Vérifications

- **52 tests unitaires** (`src/features/rbac`) · typecheck **OK** · lint **0 erreur**.
- Deux défauts trouvés **par les tests, pas par relecture** : `roleHolders(users, undefined)`
  faisait correspondre tous les comptes sans rôle (⇒ « 3 porteurs » sur un rôle vide, donc une
  suppression refusée sans raison lisible) ; et deux dialogues synchronisaient leurs props dans
  l'état par `useEffect` (rendus en cascade) — remplacés par une réinitialisation par `key`.
- Trois défauts de mise en page corrigés sur la maquette, **mesurés** en navigateur : le `37px`
  codé en dur d'AP-12 n'était pas la hauteur du bandeau (46 px, 77 px en étroit) et faisait
  rogner le logo ; le pied de la modale sortait de l'écran ; le recalage ne rejouait pas à
  l'ouverture d'un dialogue.
- 🪤 **Le générateur de barils de l'IDE a encore frappé** : 6 `index.ts` fantômes, dont deux
  sous `src/app/` où l'App Router ne les utilise jamais. Ils sont **git-ignorés**, donc
  invisibles de `git ls-files --others --exclude-standard` — mais **`tsc` les compile**.
  Diagnostic : `git ls-files --others --ignored --exclude-standard -- 'src/**/index.ts'`.

### Reste à faire (hors périmètre, tracé)

- ⚠️ **Le décompte d'administrateurs actifs est calculé sur la PAGE, pas sur le parc.**
  Approximation assumée et bornée : l'anti-lockout de l'IdP fait autorité (409 évalué dans la
  transaction). Sur un parc à plusieurs pages, un second administrateur en page 2 grise le
  bouton à tort — plus prudent que l'inverse. Une route de comptage la lèverait.
- L'e2e ne peut pas **ouvrir une session de délégué** : un invité est `INVITED` et ne peut pas
  se connecter avant d'avoir honoré son invitation (jeton à lire dans un e-mail), et un jeton
  RS256 ne se forge pas côté test. La délégation est donc prouvée par les **refus réels** sur
  les routes, pas par une session restreinte. Un seed d'opérateur délégué **activé** côté
  backend la rendrait démontrable de bout en bout.

---

## Integration Gate (DoD étendue)

Conformément à la règle posée le 2026-07-12 (`frontend-backend-integration-gate`) :

1. Types **générés** depuis l'OpenAPI réel de l'IdP — aucun type feature-local supposé.
2. Chaque contrat supposé confronté au **vrai endpoint** (payload, enums, pagination, 400/403/404/409).
3. **Zéro mock** sur le chemin réel.
4. Tests de contrat (happy path + 403 anti god-mode + 409/refus anti-lockout).
5. Tout écart tracé en ticket backend **avant** de clore l'epic.
