# Story FE-054 : e2e Atelier — PME (cahiers/OCR) + cabinet (Sage) → balance → fiscal → handoff Bilan 🏁

Status: blocked (backend non livré)

**Epic :** FE-EPIC-007 — Atelier Balance
**Points :** 3 · **Sprint :** 15 (programme) · **App :** `prospera-frontend-expert-comptable`
**API :** stack docker complet (`balance-service` + `document-service` + `bilan-service`) — Playwright, aucun mock
**Backend d'appui :** **STORY-100 (e2e Atelier docker)** — **ready-for-dev (NON livrée)**
**Réf. plan :** `frontend-sprint-status.yaml` S15 · jalon Module Atelier
**Dépendances :** FE-040→FE-053 (toute la chaîne Atelier), FE-027 (handoff), FE-028+ (Bilan)
**Maître Scrum (frontend) :** MightyRaven

---

## Convention Git

- **Une story = une branche.** Branche : `fe-054`. Commits préfixés `FE-054`.
- Branche depuis `dev` **puis rebase sur `origin/dev`** ; PR vers `dev`.

---

## ⚠️ Bloquée par le backend

STORY-100 (e2e Atelier docker) n'est **pas livrée** (backend `ready-for-dev`). Story prête à cadrer, mais son **contrat sera relevé à
la source** à la livraison — ne pas coder contre un contrat supposé. Jalon d'intégration : dépend de la livraison de **toute** la chaîne Atelier EXTENDED (FE-040→053) et de leurs backends.

---

## Convention Maquette (préalable UI)

- **Maquette validée AVANT implémentation** : — (story de test, pas d'écran).

---

## User Story

En tant que **équipe**,
je veux **un test automatisé qui prouve les deux parcours de production de balance de bout en bout**,
afin de **ne jamais casser en silence la voie cahiers/OCR ni la voie Sage.**

---

## Contexte

Miroir Atelier de l'e2e Bilan (FE-039) et de la boucle FE-017 : Playwright contre le **stack docker réel**, deux parcours — **PME** (cahiers/OCR → rattachement → balance) et **cabinet** (Sage → balance) — jusqu'au **fiscal** et au **handoff vers `bilan-service`**. Attendre sur **condition** (propagation asynchrone), jamais `waitForTimeout`.

---

## Périmètre

**Inclus**

- Parcours **PME** : cahiers (FE-043/044) + OCR (FE-045) → rattachement (FE-046) → balance canonique.
- Parcours **cabinet** : import Sage (FE-025) → balance canonique.
- Prolongement **fiscal** (FE-050/051) et **handoff** vers le Bilan (FE-027 → FE-028+).
- Attentes **sur condition** (propagation Kafka/handoff), zéro `waitForTimeout`.

**Hors périmètre**

- Le détail de chaque écran (couvert par sa story).
- Les tests unitaires (par story).

---

## Critères d'acceptation

1. Les **deux parcours** produisent une balance canonique équilibrée, prouvés de bout en bout.
2. Le prolongement fiscal et le handoff Bilan sont exercés sur le **vrai** stack, sans mock.
3. Le test est **stable** (attentes sur condition) et tourne en CI.
4. Vert sur le stack docker réel ; documenté comme jalon du Module Atelier.

---

## Definition of Done

- [ ] Contrat backend **relevé à la source** avant de coder (STORY-100 (e2e Atelier docker) livrée).
- [ ] Tous les AC validés ; `lint`/`typecheck`/`test`/`build` verts.
- [ ] Zéro mock sur le chemin réel ; types générés depuis l'OpenAPI.
- [ ] Statut mis à jour dans les trackers.
- [ ] Commits sur `fe-054`, préfixés `FE-054`.

---

## Notes

- Créée le 2026-07-24. Reprend l'ancienne ligne `FE-A18` (renumérotée en format plat).
