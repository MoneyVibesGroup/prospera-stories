# Story FE-042 : Détermination des deux axes — système comptable (SN/SMT) + régime fiscal

Status: blocked (backend non livré)

**Epic :** FE-EPIC-007 — Atelier Balance
**Points :** 3 · **Sprint :** 11 (programme) · **App :** `prospera-frontend-expert-comptable`
**API :** balance-service (`:3007`) — `GET /referentiels/actifs` (STORY-078, **livré**) + proposition d'axes STORY-080
**Backend d'appui :** **STORY-080** — **ready-for-dev (NON livrée)**
**Réf. plan :** `frontend-sprint-status.yaml` S11 · FR-A03 (STORY-078 partie référentiel = done)
**Dépendances :** FE-040 (profil société : pays, objet, CA)
**Maître Scrum (frontend) :** MightyRaven

---

## Convention Git

- **Une story = une branche.** Branche : `fe-042`. Commits préfixés `FE-042`.
- Branche depuis `dev` **puis rebase sur `origin/dev`** ; PR vers `dev`.

---

## ⚠️ Bloquée par le backend

STORY-080 n'est **pas livrée** (backend `ready-for-dev`). Story prête à cadrer, mais son **contrat sera relevé à
la source** à la livraison — ne pas coder contre un contrat supposé. ⚠️ Nuance : le **paramétrage effectif** (`GET /referentiels/actifs`, STORY-078) est **déjà livré** et consommable ; c'est la **proposition automatique des deux axes** (STORY-080, selon pays + objet + CA) qui n'est pas livrée.

---

## Convention Maquette (préalable UI)

- **Maquette validée AVANT implémentation** : choix des deux axes avec proposition justifiée + choix libre.

---

## User Story

En tant que **comptable de cabinet**,
je veux **voir le système comptable (SN/SMT) et le régime fiscal proposés selon la société, et pouvoir les ajuster**,
afin de **partir du bon paramétrage sans le deviner, tout en gardant la main.**

---

## Contexte

Deux axes structurent tout l'Atelier : le **système comptable** (SN normal / SMT minimal) et le **régime fiscal**. STORY-080 les **propose** selon pays + objet + CA ; l'humain **valide**. Le référentiel effectif qui en découle est déjà lisible via `GET /referentiels/actifs` (STORY-078, livré) — checksums vérifiés inclus.

---

## Périmètre

**Inclus**

- Proposition des deux axes **justifiée** (pourquoi ce système / ce régime).
- Choix **libre** possible (la proposition n'est pas une contrainte).
- Lecture du **paramétrage effectif** (`GET /referentiels/actifs`) : référentiel comptable + paquet fiscal, état du cache, checksums.
- Gestion honnête de l'indisponibilité (409 `REFERENTIEL_UNRESOLVED`/`REFERENTIEL_NON_PACKAGE`, 502, 503 — au contrat STORY-078).

**Hors périmètre**

- Le calcul fiscal (FE-050→052).
- L'édition du profil (FE-040).

---

## Critères d'acceptation

1. Les deux axes sont **proposés et justifiés**, et restent **modifiables**.
2. Le paramétrage effectif est affiché depuis `GET /referentiels/actifs`, avec intégrité (checksum) et état du cache.
3. Un référentiel non résolu / non packagé est **dit proprement**, jamais une page cassée.
4. Types générés ; i18n FR ; tests unitaires + E2E.

---

## Definition of Done

- [ ] Contrat backend **relevé à la source** avant de coder (STORY-080 livrée).
- [ ] Tous les AC validés ; `lint`/`typecheck`/`test`/`build` verts.
- [ ] Zéro mock sur le chemin réel ; types générés depuis l'OpenAPI.
- [ ] Statut mis à jour dans les trackers.
- [ ] Commits sur `fe-042`, préfixés `FE-042`.

---

## Notes

- Créée le 2026-07-24. Reprend l'ancienne ligne `FE-A06` (renumérotée en format plat).
