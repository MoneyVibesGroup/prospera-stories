# Story AP-09 : Mot de passe oublié — récupération de compte pour les opérateurs plateforme (console admin)

Status: review

**Epic :** AP-EPIC-000 — Socle admin & sécurité
**Points :** 2 · **Sprint :** 9 · **App :** `frontend-admin-panel`
**API :** auth-service (:3001) — `POST /api/v1/auth/forgot-password` · `POST /api/v1/auth/reset-password` (**publiques**), **proxifiées par le BFF** (même patron que le login d'AP-01)
**Backend d'appui :** **STORY-125** (mot de passe oublié, tous personas) — ✅ **LIVRÉE** (`MNV-125`, mergée sur `origin/dev` d'`auth-service` le 2026-07-22)
**Réf. plan :** `frontend-sprint-status.yaml` (S9) · `docs/tech-spec-admin-panel-2026-07-10.md`
**Backend prêt :** ✅ **OUI.** Les deux routes sont servies et `@Public()` :

```
POST /api/v1/auth/forgot-password   → 202 SYSTÉMATIQUE (anti-énumération), throttle 5/min
POST /api/v1/auth/reset-password    → 204 · 400 (jeton mort OU mot de passe refusé) · 429
```
**Dépendances :** **AP-01** (socle console + login `PLATFORM_ADMIN` + BFF) — sprint 3
**Story sœur :** **FE-020** (même parcours dans l'app cliente cabinet)
**Maître Scrum (frontend) :** MightyRaven

---

## Convention Git

- **Une story = une branche.** Branche prévue : `ap-09`.
  ⚠️ **Dérogation assumée à la livraison (2026-08-08, demande du PO)** : AP-09 et AP-12 partagent la
  branche **`AP-09-12`**. Les deux étaient au même état — maquette mergée, code absent — et le PO a
  demandé de les reprendre ensemble. À garder à l'esprit à la revue : **la PR porte deux stories**.
- **Commits préfixés `AP-09`** — ex. `AP-09 mot de passe oublié (console admin)`.
- Dépôt `frontend-admin-panel` ; PR vers `dev`.

---

## Convention Maquette (préalable UI)

- **Maquette validée AVANT implémentation** (2 écrans : demande / réinitialisation, + l'entrée depuis l'écran
  de connexion d'AP-01), conforme au Design System Prospera, publiée en Artifact.
- **Ne pas coder tant que la maquette n'est pas validée.**

---

## User Story

En tant qu'**opérateur de la plateforme Money Vibes** (`PLATFORM_ADMIN`, `PLATFORM_KYC_OFFICER`,
`PLATFORM_SUPPORT`, `PLATFORM_AUDITOR`),
je veux **retrouver l'accès à la console quand j'ai oublié mon mot de passe**,
afin de **ne pas dépendre d'une intervention manuelle en base de production — la seule issue qui existe aujourd'hui**.

---

## Contexte

**Demande explicite du PO (2026-07-22)** : le déblocage doit exister « pour le cabinet **mais aussi au niveau
de l'admin-panel** ».

C'est ici que le trou est le plus grave. Vérifié dans le code de l'IdP :

- il n'existe **ni** `forgot-password`, **ni** `reset-password` ;
- **aucun `PLATFORM_ADMIN` ne peut réinitialiser le mot de passe d'un opérateur** :
  `platform-users.controller.ts` expose uniquement `POST` (invitation org-less), `GET` et `PATCH :id` (**rôle**) ;
- les comptes plateforme sont **seedés** (`seed-platform-admin.ts`) ou créés par **invitation org-less**
  (STORY-104).

⇒ Un `PLATFORM_KYC_OFFICER` qui oublie son mot de passe est **irrécupérable**, sauf à rejouer un seed **à la
main, en production**. C'est une **impasse d'exploitation**, pas un inconfort.

Le backend est **STORY-125**, qui traite les **deux publics avec le même code** — la console n'a donc **aucune
logique d'identité** à écrire (elle n'en possède aucune : c'est un **BFF sans base**).
✅ **STORY-125 est livrée** (2026-07-22) : le blocage est levé, cette story est délibérément mince —
**2 pages + 2 handlers de pass-through**.

---

## Portée

- **2 Route Handlers dans le BFF** proxifiant `POST /auth/forgot-password` et `POST /auth/reset-password` vers
  auth-service — **même patron que le login d'AP-01**, mais **sans jeton** (routes publiques : ne pas les
  placer derrière la garde `PLATFORM_ADMIN`, elles sont appelées **par définition** par quelqu'un qui n'est pas
  connecté).
- **Écran « Mot de passe oublié »** (adresse e-mail) + **écran « Réinitialiser »** (`?token=…`), aux couleurs de
  la console.
- **Entrée depuis l'écran de connexion** d'AP-01 : lien « Mot de passe oublié ? ».
- **i18n / design system** : réutilise le socle partagé avec l'app cliente (`apps.admin.shared_socle`).

⚠️ **Ne pas inventer le schéma** : `ForgotPasswordDto` / `ResetPasswordDto` viennent de l'**OpenAPI généré**,
et la règle de robustesse du mot de passe est **celle du backend** (validateur partagé avec `RegisterDto`).

**Hors périmètre :** toute réinitialisation **par un administrateur** pour le compte d'un tiers (écartée du
périmètre backend — un opérateur qui remet le mot de passe d'un autre pourrait détourner son compte) ·
la gestion des opérateurs (**AP-08**) · le même parcours côté cabinet (**FE-020**).

---

## Critères d'acceptation

1. Depuis l'écran de connexion de la console, un lien « Mot de passe oublié ? » mène à l'écran de demande.
2. Demande envoyée → message **non affirmatif** : ⚠️ **« Si un compte existe pour cette adresse, un lien vient
   d'y être envoyé. »** — le backend répond **202 même pour une adresse inconnue** (anti-énumération, AC-05 de
   STORY-125). La console **ne doit ni confirmer ni infirmer** l'existence d'un compte opérateur : c'est
   d'autant plus sensible ici que l'annuaire visé est celui des **employés de Money Vibes**.
3. ⚡ **Le lien reçu par e-mail ramène bien sur LA CONSOLE**, pas sur l'app cabinet — le backend déduit la cible
   du compte (`platformRole` renseigné → `PASSWORD_RESET_URL_PLATFORM`, AC-03 de STORY-125).
   **Vérifié en bout-en-bout, lien lu dans Mailhog.** C'est le critère qui distingue cette story de FE-020.
4. Réinitialisation réussie → **204** → redirection vers la connexion de la console → l'opérateur se reconnecte
   et **retrouve ses `perms[]`** (le masquage d'UI d'AP-08, s'il est livré, fonctionne de nouveau).
5. Token absent de l'URL → écran d'erreur explicite, **aucun** appel réseau. Token invalide/expiré/déjà utilisé
   → **400** → message clair avec un lien pour **refaire une demande**.
6. Mot de passe trop faible → **400** au niveau du champ, avec la règle **du backend**. **429** rendu comme un
   throttle, pas comme une panne.
7. ⚡ **Les 2 écrans et les 2 routes du BFF sont publics** : accessibles **sans session**, **non redirigés** par
   la garde `PLATFORM_ADMIN` d'AP-01, et **non proxifiés avec un jeton**. C'est le piège n°1 de cette story —
   une console dont la garde renvoie tout anonyme vers `/login` rendrait la récupération **inatteignable
   précisément quand on en a besoin**. **Test dédié.**
8. Le BFF **ne journalise ni le corps des requêtes ni le token** (le token de réinitialisation vaut le compte).
9. Tests : unitaires (schémas des 2 formulaires, cas 400/429, token manquant) + **le test d'accessibilité sans
   session** (AC-7) + un test sur les Route Handlers (pass-through des statuts 202/204/400/429).

---

## Integration Gate (DoD étendue)

1. Types **générés** — aucun type supposé.
2. Contrat confronté au vrai endpoint : statuts **202 / 204 / 400 / 429**.
3. **Zéro mock** sur le chemin réel (stack docker STORY-075).
4. **Parcours prouvé de bout en bout sur un compte PLATEFORME** (seedé ou invité org-less) : demande depuis la
   console → **lien lu dans Mailhog, pointant sur l'origine de la console** → réinitialisation → reconnexion à
   la console. Sans cette preuve, la story n'est pas finie — c'est exactement le scénario qui n'avait **aucune
   issue** auparavant.
5. Tout écart tracé en ticket backend.

---

## Notes

- **Aucune duplication de logique** : une seule implémentation backend (STORY-125) sert les deux consoles. AP-09
  est délibérément mince — 2 pages + 2 handlers de pass-through.
- **Le front ne déclare jamais son URL de retour** : la cible est une **configuration serveur**
  (`PASSWORD_RESET_URL_PLATFORM`) et le backend **rejette** tout champ de redirection dans le corps
  (**400**, AC-04 de STORY-125) — garde anti-redirection ouverte.
- **Port** : la console tourne sur un port distinct de l'app cliente (:3110 recommandé par AP-01) ; c'est cette
  origine qui doit être configurée côté backend. Le BFF étant **same-origin**, aucune contrainte CORS.
- **Ce qui reste ouvert :** l'opérateur qui a perdu **l'accès à sa boîte e-mail** — non couvert, et la
  réinitialisation par un tiers a été écartée. À rouvrir seulement si le cas se présente réellement.

---

## Dev Agent Record

### Agent Model Used

claude-opus-5 (Claude Code)

### Debug Log References

- Branche **`AP-09-12`** (poussée le 2026-08-08), commit `aa6cef7`. ⚠️ **Branche commune avec
  AP-12**, à la demande du PO — dérogation assumée à « une story = une branche ».
- `npm run typecheck` — 0 erreur.
- `npm run lint` — **0 erreur**, 3 avertissements pré-existants
  (`_grantedBy`, `QueueItemDto`, `react-hooks/incompatible-library` de `version-form-dialog`).
- `npm run test` — **450 tests verts / 52 fichiers** (406 → 450, AP-09 + AP-12 confondus).
- `npm run build` — **15 routes**, dont les 2 pages publiques et les 2 Route Handlers.
- `npm run gen:api` — types **régénérés** depuis l'OpenAPI réel d'`auth-service` :
  `ForgotPasswordDto` et `ResetPasswordDto` sont désormais des types **générés**, avec garde de
  compilation dans `password-reset.schema.ts` (patron FE-INT-4).
- `e2e/recovery-and-deposit.spec.ts` — **6 specs vertes** en vrai navigateur contre le stack docker.

### Completion Notes List

**Le point de départ : la maquette existait, le code non.** `1e86cad` (PR #12) n'ajoutait qu'un
fichier — `docs/mockups/ap-09-mot-de-passe-oublie.html`, 1719 lignes. Zéro ligne sous `src/`, et la
branche `origin/ap-09` était **identique à `dev`**. Le tracker disait vrai : « reste le code ».

**AC-7 — la propriété qui ne se voit pas dans un composant.** Ce qui rend une page privée dans l'App
Router n'est pas une ligne de son code : c'est **son emplacement**. Le layout de `(app)` monte
`RequirePlatformAdmin` au-dessus de tout ce qu'il contient. Les deux écrans vivent donc à la racine
de `src/app/`, et un test **structurel** (`src/test/public-recovery-routes.test.ts`) fige à la fois
l'emplacement, l'absence de garde dans les pages, l'absence de Bearer/cookie dans les handlers et
l'absence de journalisation — plus une assertion que la garde existe **toujours** sur `(app)`, sans
quoi les trois premières prouveraient l'absence d'une protection disparue.
⚠️ Ce test filtre les **commentaires** avant d'inspecter : sans ce filtre, il interdirait d'écrire
la documentation de la règle qu'il fait respecter.

**⚡ Le 400 recouvre DEUX causes, et les confondre crée une impasse.** Un jeton mort demande de
refaire une demande ; un mot de passe refusé demande de corriger un champ. Renvoyer « refaites une
demande » à quelqu'un dont le jeton est **valide** lui fait perdre le seul jeton qu'il avait.
Le tri se fait sur la **forme du corps**, pas sur son texte — `message` en tableau = `ValidationPipe`
(mot de passe), `message` simple = `BadRequestException` (jeton) :

```
{"statusCode":400,"message":["newPassword must be longer than or equal to 8 characters"]}
{"statusCode":400,"message":"Lien de réinitialisation invalide ou expiré. Veuillez refaire une demande."}
```

Les deux formes ont été **relevées sur l'IdP réel**, pas supposées. Se fier au texte français
reviendrait à dépendre d'une chaîne d'affichage : elle changera, et la branche mourrait en silence.

**Anti-énumération rendue testable.** Le message est conditionnel (« **si** un compte existe »), et
un test compare le rendu des deux parcours — adresse connue / adresse inconnue — pour vérifier
qu'ils sont identiques **au caractère près**. C'est la seule façon de tester une non-fuite : on ne
peut pas prouver l'absence d'un signal assertion par assertion, on peut prouver l'égalité.

**Le BFF filtre le corps.** Un `redirectUrl` glissé par l'appelant ne franchit pas le Route Handler.
Il serait de toute façon refusé en 400 par l'IdP (garde anti-redirection ouverte de STORY-125), mais
il n'a aucune raison de traverser — test dédié.

**Le chemin `/reinitialiser-mot-de-passe` est un contrat de configuration**, pas un choix
d'ergonomie : `PASSWORD_RESET_URL_PLATFORM` y pointe dans le compose racine. Le renommer casserait
tous les liens **déjà envoyés**, sans qu'aucun test de rendu ne bronche. Le test structurel le fige
pour cette raison, et le commentaire de la page le dit.

**L'écran de connexion d'AP-01 n'est touché que d'une ligne** — le lien, sur la ligne du libellé du
champ mot de passe. Conforme à la maquette : « AP-09 touche `login-form.tsx` sur une seule ligne ».

### Integration Gate (DoD étendue) — exécutée

| # | Exigence | Résultat |
|:--:|---|---|
| 1 | Types **générés** | ✅ `npm run gen:api` — `ForgotPasswordDto`/`ResetPasswordDto` générés + garde de compilation |
| 2 | Statuts **202 / 204 / 400 / 429** confrontés | ✅ les quatre, contre l'IdP réel |
| 3 | Zéro mock sur le chemin réel | ✅ stack docker |
| 4 | **Parcours prouvé de bout en bout sur un compte PLATEFORME** | ✅ voir ci-dessous |
| 5 | Écart tracé en ticket | ✅ aucun écart de contrat ; un écart d'**environnement** relevé (infra, pas backend) |

**Le parcours du point 4, dans l'ordre, sur `admin@prospera.local` :**

1. Demande depuis `/mot-de-passe-oublie` → **202**. Rejouée sur une adresse inconnue → **202**, corps
   identique. L'annuaire des opérateurs n'est pas énumérable depuis la console.
2. **Lien lu dans Mailhog** → il pointe sur **`:3110/reinitialiser-mot-de-passe?token=…`**.
   ⚡ **C'est le critère qui distingue AP-09 de FE-020**, et il est vérifié : la cible est déduite du
   compte **côté serveur** (`platformRole` renseigné → `PASSWORD_RESET_URL_PLATFORM`). Rien dans le
   front ne peut l'influencer, donc rien dans le front ne pouvait le vérifier — d'où la lecture du
   mail.
3. Réinitialisation → **204**. Rejeu du **même** jeton → **400** : usage unique confirmé.
4. Reconnexion → **200**, et le jeton porte bien ses 13 `perms[]` dont `referentiel:publish`.

⚠️ **Le mot de passe a été remis à sa valeur d'origine** : l'environnement de dev est inchangé après
la vérification.

**⚠️ Écart d'ENVIRONNEMENT relevé — hors story, à trancher par l'infra.** Le `.env.local` du dépôt
pointe les services sur une IP de LAN (pour tester depuis un autre appareil). Cette adresse est
**injoignable depuis la machine de dev elle-même** : le BFF y prend un `UND_ERR_CONNECT_TIMEOUT` sur
**toutes** ses routes d'auth — **le login d'AP-01 compris**, ce n'est donc pas une régression de
cette story. La vérification a été menée avec un override `localhost` au build. À décider : deux
profils d'env, ou une IP joignable des deux côtés.

**Ce qui reste ouvert, et qui n'est pas de cette story :** l'opérateur qui a perdu **l'accès à sa
boîte e-mail** — non couvert, et la réinitialisation par un tiers a été écartée du périmètre
backend. À rouvrir seulement si le cas se présente réellement.

### File List

**Créés**

- `src/app/api/auth/forgot-password/route.ts` *(+ `route.test.ts`)*
- `src/app/api/auth/reset-password/route.ts` *(+ `route.test.ts`)*
- `src/app/mot-de-passe-oublie/page.tsx`
- `src/app/reinitialiser-mot-de-passe/page.tsx`
- `src/components/layout/auth-shell.tsx` — coque des écrans publics
- `src/features/auth/api/password-reset-client.ts`
- `src/features/auth/schemas/password-reset.schema.ts` *(+ `.test.ts`)*
- `src/features/auth/components/forgot-password-form.tsx` *(+ `.test.tsx`)*
- `src/features/auth/components/reset-password-form.tsx` *(+ `.test.tsx`)*
- `src/test/public-recovery-routes.test.ts` — **le test dédié d'AC-7**
- `e2e/recovery-and-deposit.spec.ts` — Integration Gate *(partagé avec AP-12)*

**Modifiés**

- `src/features/auth/components/login-form.tsx` — le lien « Mot de passe oublié ? » *(une ligne)*
- `src/i18n/messages/fr.json` — `auth.forgotPassword.*`, `auth.resetPassword.*`, `auth.validation.*`
- `src/types/api/auth.ts` — **régénéré**
