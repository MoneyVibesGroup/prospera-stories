# Story AP-05 : Entitlements (octroi / mise à jour / révocation module × org × référentiel)

Status: blocked (backend)

**Epic :** AP-EPIC-002 — Entitlements & catalogue
**Points :** 5 · **Sprint :** 4 (programme) · **App :** `prospera-admin-panel`
**API :** platform-catalog-service (`/catalog/entitlements`) via BFF admin-panel · **Backend d'appui :** STORY-033 (entitlements grant/update/revoke + réconciliation), STORY-034 (`entitlement.changed`), STORY-048 (grant proxifié)
**Réf. plan :** `docs/frontend-sprint-status.yaml` · `docs/frontend-sprint-plan-admin-panel-2026-07-12.md`
**Backend prêt :** ⚠️ **partiellement** — les écritures sont proxifiées (STORY-048), **la lecture ne l'est pas** : voir §Statut
**Dépendances :** AP-02 (fiche org), AP-04 (catalogue), **STORY-143** (lecture proxifiée)
**Maître Scrum (frontend) :** MightyRaven

---

## ⚠️ Statut : BLOQUÉE (requalifiée le 2026-07-28)

Cette story était marquée `ready-for-dev`. Elle ne l'est pas : **son tout premier critère
d'acceptation — « liste des entitlements d'une org » — n'a aucune route à appeler.**

vérifié le 2026-07-28 sur `origin/dev` de `prospera-admin-panel-service` (commit `0521258`), le BFF proxifie les **écritures** et rien d'autre :

| Route BFF | |
|---|---|
| `PUT /admin/orgs/:orgId/entitlements/:moduleCode` | octroi ✅ |
| `DELETE /admin/orgs/:orgId/entitlements/:moduleCode` | révocation ✅ |
| *(aucune)* | **lecture ❌** |

L'amont existe pourtant (`GET /entitlements/:orgId` sur `platform-catalog-service`) : il ne manque
que le pass-through. Le front admin ne parle qu'au BFF.

**Levée du blocage : STORY-143** (sprint 18 backend). ⚡ Cette route-là **ne dépend de rien** et peut
être livrée seule, avant le reste de la série — c'est le chemin le plus court pour rouvrir AP-05.

---

## Convention Git

- **Une story = une branche.** Branche : `ap-05`. Commits préfixés `AP-05`.

---

## Convention Maquette (préalable UI)

- **Maquette validée AVANT implémentation** (gestion des entitlements d'une org), conforme au Design System Prospera, publiée en Artifact.

---

## User Story

En tant qu'**administrateur plateforme Money Vibes**,
je veux **octroyer, mettre à jour ou révoquer les droits d'usage (entitlements) d'un module pour une organisation**,
afin d'**« allumer » les modules que le client peut utiliser (ex. Bilan avec le référentiel SYSCOHADA)**.

---

## Contexte

**Le geste central de la tour de contrôle** : octroyer un entitlement `ACTIVE` pour `(org × module × référentiel/version)` déclenche `entitlement.changed` (STORY-034) → le read-model de l'app cliente s'actualise → **le module apparaît** chez l'utilisateur (FE-014). Sous dogfooding (DG-1), c'est **ici** qu'on active le Bilan pour Money Vibes, **sans passer par le paiement**.

---

## Périmètre

**Inclus :**
- Depuis la fiche org (AP-02) : **liste des entitlements** de l'org (module, version, référentiel, statut ACTIVE/REVOKED).
- **Octroyer** : choisir un module + version + référentiel (depuis le catalogue AP-04) → grant `ACTIVE`.
- **Mettre à jour** (version/référentiel) et **révoquer** un entitlement.
- **Vue de réconciliation** (état effectif) + **validation contre le catalogue** (pas d'octroi d'un couple inexistant).
- Feedback + rafraîchissement ; note de cohérence éventuelle (l'app cliente suit via l'événement).

**Hors périmètre :**
- Définition du catalogue (AP-04).
- Séquence de provisioning multi-modules d'un vertical (AP-06, qui réutilise cette story).
- Facturation/plans (Module 2).

---

## Critères d'acceptation

- [ ] Liste des entitlements d'une org (module/version/référentiel/statut).
- [ ] **Octroyer** un entitlement (module + version + référentiel valides au catalogue) → `ACTIVE`.
- [ ] **Mettre à jour** (version/référentiel) et **révoquer** un entitlement ; état reflété.
- [ ] Validation contre le catalogue (couple inexistant refusé) ; erreurs backend affichées.
- [ ] Après octroi, message expliquant que l'activation côté client est **asynchrone** (`entitlement.changed`).
- [ ] i18n FR ; tests : grant/update/revoke, validation catalogue, réconciliation.

---

## Notes techniques

| Composant | Fichier (proposé) | Nature |
|---|---|---|
| BFF entitlements | `src/app/api/admin/entitlements/*` (proxy JWT admin) | Nouveau |
| Query/mutations | `src/features/entitlements/api/*` + `hooks/*` | Nouveau |
| Écrans | `src/features/entitlements/components/{EntitlementsPanel,GrantDialog}.tsx` | Nouveau |

**Décisions & vigilance :**
- **Effet asynchrone** : l'UI confirme l'octroi côté catalogue ; la disponibilité chez le client suit l'événement (ne pas promettre un effet instantané).
- **Cohérence catalogue** : proposer uniquement des modules/versions/référentiels existants (source AP-04) pour éviter les grants invalides.
- **Traçabilité** : chaque action (grant/revoke) est un acte admin — afficher l'auteur/horodatage si l'API les fournit.

---

## Tasks / Subtasks

- [ ] BFF entitlements + query/mutations (AC 1-3)
- [ ] `EntitlementsPanel` (liste) + `GrantDialog` (module/version/référentiel) (AC 2)
- [ ] Update/revoke + validation catalogue + réconciliation (AC 3, 4)
- [ ] Message d'effet asynchrone (AC 5)
- [ ] i18n + tests (AC 6)

---

## Definition of Done

- [ ] Critères d'acceptation validés ; tests verts.
- [ ] `lint` / `typecheck` / `test` / `build` verts (local + CI).
- [ ] Statut mis à jour dans les trackers.
- [ ] Commits sur `ap-05`, préfixés `AP-05`.

---

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
